# statnive-live — Self-Hosted & SaaS Analytics Platform

## Context

Sixteen research documents (docs 14–29), 500+ sources, 2,000+ lines of drop-in Go code. Architecture, features, schema, security, and Iranian-DC operational decisions are finalized. Docs 24 (AGPL-safe Pirsch extraction), 25 (skill install matrix), 27 (three-gap closure — WAL / CGNAT / GDPR-on-HLL), 28 (final-three-gap closure — GeoIP pipeline / Iranian DC deploy / ClickHouse ops), 29 (production load-simulation gate — 5-phase graduation matrix + generator_seq oracle + 6-scenario chaos) drive the Week 17+ schedule.

**statnive-live** serves two target markets: (1) Iranian high-traffic sites — SamplePlatform is the first customer on the Iranian-DC air-gapped deployment; (2) outside-Iran sites on the managed SaaS at `statnive.live`. See § Scope below for the precise binary / WP-plugin boundary and deployment postures.

## Scope

**This plan targets the `statnive-live` Go binary — the standalone analytics server at `statnive-live/` (separate GitHub repo, submodule of the parent workflow repo).** Same binary ships in two deployment postures:

- **Self-hosted** — operator runs `./deploy/airgap-install.sh` on their Linux server (Hetzner VPS, Asiatech Iranian DC, enterprise on-prem) with zero required outbound calls.
- **SaaS** — we run the same binary on `statnive.live` / `app.statnive.live` / `demo.statnive.live` behind Hetzner, tenanted by `site_id`.

**This is NOT the WordPress plugin.** The sibling `statnive/` submodule is a separate WP analytics plugin on a separate release cadence; Phase 8 touches none of it. The two share only the parent `statnive-workflow/` repo and the `statnive` naming prefix. Concrete: no `composer.json`, no `.phpcs.xml`, no `block.json`, no `wp-cli.yml` in scope here. Everything in this plan lives under `statnive-live/` and operates on the Go binary, its systemd unit, its iptables rules, and its air-gap install tooling.

**Reference streaming workload (StreamCo, confirmed 2026-04-19).** Two endpoints frame capacity; we ship the minimum first and ramp app-by-app.

| Envelope | Scope | Peak day events | Monthly events | Bandwidth/mo | Disk/year | EPS peak |
|---|---|---|---|---|---|---|
| **MINIMUM — P1 cutover** | Web, required events only | 3M | 75M | ~22 GB | ~36 GB | ~140 |
| **MAXIMUM — P5 full build** | All apps (web + iOS + Android + TV) | 200M | 4B | ~1.2 TB | ~1.9 TB | ~9,000 (spike ~18K) |

Minimum fits the cheapest Asiatech VPS (~15–28M Rial/mo). Maximum requires a 2–3 node Iranian-DC cluster (~800M–1.5B Rial/mo). 5-phase roadmap P1→P5 in [`../jaan-to/outputs/capacity-planning-standalone-analytics.md`](../jaan-to/outputs/capacity-planning-standalone-analytics.md).

> **Design ceiling vs. observed current-state (doc 30 reconciliation, 2026-04-21).** The MIN/MAX envelope above is the **design target** — statnive-live's load-gate acceptance (Phase 7e graduation gate per doc 29 §4) runs against MAX, not current. Doc 30 (GA4 calibration, 2026-04-20) measured SamplePlatform's observed current-state at ~80M events/day peak / ~8K EPS burst / 62% Iran + 38% diaspora over a 192-day window. We **retain doc 29's 200M / 40K / 32c-128GB P5 numbers** because (a) SamplePlatform's organic growth (Dec 2025 new-user +60% spike, doc 30 §5) will exceed 80M within 12–18 months at streaming-industry norms; (b) 192-day daily aggregates smooth away match-day + Ramadan iftar sub-daily spikes that doc 29's `match_spike()` (2.5–4×) and `ramadan_diurnal()` (1.8–2.2×) formulas correctly model; (c) ~200M Rial/mo P5 steady-state savings from right-sizing are small against the catastrophic cost of an EPS overrun during a Tehran-derby Friday evening. Doc 30's value is **load-shape realism** (bimodal sessions, 62/38 geographic split, `user_engagement` + `ui_interact` event mix, 7th chaos scenario, long-session soak, `app_exception` anti-pattern), **not capacity re-planning**. The proposed P5 downsize to 16c/64GB was rejected per user directive "design for maximum."

- **Repo:** https://github.com/statnive/statnive.live.git
- **Folder:** `statnive-live/`
- **Domains (dual-domain Architecture C, disjoint customer sets):**
  - `statnive.live` — international SaaS only (Phase A dogfood + Phase C self-serve). Outside-Iran DNS + Netcup origin. No Iranian resolver ever queries this zone.
  - `statnive.ir` — Iranian customers only (Phase B SamplePlatform + future Iranian tenants). Self-hosted NSD on Asiatech + Iranian-DC origin. No Cloudflare per [`iranian-dc-deploy`](.claude/skills/iranian-dc-deploy/README.md) `iran-no-cloudflare` rule. Optional `statnive.ایران` IDN reserved at IRNIC (v1.1).
  - Decision rationale + carve-out from [doc 26 § 3.3 + § 7.2](../jaan-to/docs/research/26-iran-shutdown-dns-strategy.md): doc 26 originally rejected dual-domain (Architecture C) for the unified-customer case; this deployment is the disjoint-customer-set carve-out where C wins on simplicity (no GeoDNS, no AXFR, Namecheap-friendly NS-only delegation per zone).

## Product Definition

**statnive-live** = Go single binary + ClickHouse analytics platform.

**Decisions locked:**
- **Greenfield build** — 100% original code. Study Pirsch fork at `~/Projects/pirsch/` for patterns; never copy AGPL source.
- **License: ALL deps MIT/Apache/BSD** — no AGPL. Sold as SaaS outside Iran where AGPL §13 applies.
- **Multi-tenant from v1** — `site_id` in schema from day 1. SamplePlatform = site_id=1.
- **Dual hosting** — D1 SaaS on Netcup VPS 2000 G12 iv NUE hourly-based (€25.48/mo + €5 one-time setup, 8 vCore AMD EPYC / 16 GB DDR5 ECC / 512 GB NVMe / Nuremberg, procured 2026-04-24 per research doc 36 with Hetzner-doc-verification fallback; Hetzner AX42 at €46/mo remains the Phase C growth-tier architectural target once doc-verification activates), D2 SamplePlatform production on Iranian DC (~€180/mo).

Two distribution models from day 1 — same binary, multi-tenant via `site_id` + `WHERE site_id = ?`:

| Model | Description | Revenue |
|-------|-------------|---------|
| **Self-hosted** | Customer runs statnive-live on their own server | License fee; manual activation |
| **SaaS (managed)** | We host on Hetzner (outside Iran only) | Monthly subscription by pageviews |

## Repository Structure

Top-level tree:

```
statnive-live/
├── CLAUDE.md           # Project rules
├── .claude/skills/     # 14 custom + 49 community skills
├── cmd/statnive-live/  # Entry point
├── internal/           # config, audit, cert, ratelimit, ingest, enrich, identity, storage, sites, health, cache, dashboard, auth
├── web/                # Preact SPA (Phase 5)
├── tracker/            # <2KB IIFE tracker (Phase 4)
├── config/             # YAML defaults + sources.yaml (60+ Iranian referrers)
├── deploy/             # systemd, iptables, backup, airgap scripts
├── vendor/             # Vendored Go deps (offline builds)
├── docs/               # rules/, history/, tech-docs/, tooling.md, brand.md, cli-operator-surface.md, deployment.md, tech-docs-index.md, repo-structure.md
├── test/               # integration, enrichment, multitenant, security, dashboard tests
└── Makefile            # build, test, lint, audit, airgap-bundle
```

Full tree with `[shipped]`/`[planned]`/`[scaffolded]` per-file markers in [`docs/repo-structure.md`](docs/repo-structure.md).

## Development Phases

### Status — 2026-04-26

| Phase | Status | Notes |
|---|---|---|
| **0 — Project setup** | ✅ Complete | PR #1. Repo, Makefile, CI, schema, vendoring live. |
| **1 — Ingestion pipeline** | ✅ Complete | PR #2. Real 6-stage enrichment, BLAKE3 + IRST salt, 18 MB bloom + cross-day grace, 17-step channel tree, 503 back-pressure. Burst guard PR #14. |
| **2a — Surface hardening** | ✅ Complete | PR #6. TLS 1.3 manual PEM + SIGHUP reload + expiry watcher; httprate rate limit (NAT-aware, audit-instrumented); JSONL audit file sink; FastRejectMiddleware. |
| **2b — Auth + RBAC** | ✅ Complete | bcrypt cost-12 + 32-byte `crypto/rand` session tokens + SameSite=Lax HttpOnly cookies + 14-day TTL + admin/viewer/api RBAC. Migration 004 adds `statnive.users` + `statnive.sessions` (ReplacingMergeTree, `{{if .Cluster}}` templated, `ORDER BY site_id, email` for login hot path). `internal/auth/` ships: types + password (with dummyHash timing-attack defense) + sessions + CachedStore (60 s LRU + cascade-revoke on password/role/disable, CVE-2024-10924 class) + middleware (session + api-token composite) + RBAC + handlers (`POST /api/login` / `POST /api/logout` / `GET /api/user`) + first-run bootstrap via env. Per-IP login limiter (10/min) + per-email lockout (10 fails / 15 min → 5 min) defend against distributed credential stuffing. Login body uses `DisallowUnknownFields` (Verification §52 template for Phase 3c) + uniform `{"error":"invalid credentials"}` response (no user enumeration). `auth-return-nil-guard` Semgrep rule + fixtures under `.claude/skills/blake3-hmac-identity-review/` + `make identity-gate` target; unit-test regression (`internal/auth/nilguard_test.go`) is the hard gate. SPA swapped to cookie-auth via `credentials: 'include'` + new `web/src/pages/Login.tsx` (~1 KB gz, initial-JS budget bumped 13→14 KB). Legacy `dashboard.bearer_token` auto-promoted to `auth.api_tokens` so Phase 3b smoke + Phase 5c e2e bearer paths keep passing. Smoke harness (`make smoke`) extended with `probe_login_flow`; Playwright `web/e2e/auth.spec.ts` extended with login / logout / mass-assignment-guard tests. Docs: `docs/runbook.md` ships SOPs for bootstrap, password rotation, session revoke, API-token provisioning, local-dev Secure=false. Unblocks Phase 3c admin CRUD + Phase 11 SaaS signup. |
| **2c — Operational hardening** | ✅ Complete | PR #36. `deploy/systemd/statnive-live.service` — hardened unit (NoNewPrivileges / ProtectSystem=strict / PrivateTmp / CapabilityBoundingSet=CAP_NET_BIND_SERVICE / SystemCallFilter=@system-service / RestrictAddressFamilies + namespace isolation); `deploy/systemd/harden-verify.sh` grep gate (no systemd dependency) invoked by `make systemd-verify`. `deploy/iptables/rules.{v4,v6}` default-deny INPUT/FORWARD + 22/80/443/ICMP echo allow; IPv6 carries the ICMPv6 ND carve-outs. `docs/luks.md` operator guide — when-to-enable (shared VPS / laptop), when-to-skip (dedicated cage + encrypted backups), `cryptsetup luksFormat --type luks2 --cipher aes-xts-plain64 --key-size 512` setup, `/etc/crypttab` + `/etc/fstab` persist, key custody (1Password primary + off-premises recovery), clevis/TPM flagged v1.1. Backup automation: `deploy/backup/{config.yml,config-drill.yml,config-ci.yml}` (prod / drill-host / CI-MinIO variants) + `deploy/backup/drill.sh` (POSIX; `list remote` → `restore_remote` → per-table row-count parity against `EXPECT_ROWS` snapshot → `countMerge(visitors_hll_state) FINAL` rollup mergeability; non-zero exit on any FAIL). `test/seed/backup-drill.sh` seeds 10 K synthetic events across 2 site_ids + 4 paths. `.github/workflows/backup-drill-nightly.yml` runs the round-trip via `altinity/clickhouse-backup:v2.5.20` sidecar + MinIO; **`workflow_dispatch`-only** (nightly schedule demoted in PR #41 — see `v1.1-ci-drill` row for re-enable conditions). Host-side operator cron is the release-blocking SoT per `docs/runbook.md` § Backup & restore. Makefile targets: `systemd-verify`, `seed-backup-drill`, `backup-drill-local`. |
| **3a — Dashboard query foundation** | ✅ Complete | PR #9. Filter + Store + 7 v1 queries (Overview / Sources / Pages / SEO / Campaigns / Trend / Realtime) + LRU + tenancy-grep gate. Geo / Devices / Funnel stubs return ErrNotImplemented. |
| **3b — Dashboard HTTP layer** | ✅ Complete | PR #12. 9 stat handlers in `internal/dashboard/stats.go` (Overview / Sources / Pages / SEO / Trend / Campaigns / Geo / Devices / Funnel — the last three return 501 until v1.1/v2) + realtime handler in `realtime.go` + IRST Filter + bearer-token stub + WITH FILL. |
| **3c — Admin CRUD** | ✅ Complete | PR #35. `/api/admin/users` (list/create/update/password/disable/enable) + `/api/admin/goals` (list/create/update/disable, soft-delete). Migration 005 ships `statnive.goals` (ReplacingMergeTree, `{{if .Cluster}}` templated, v1 `Enum8('event_name_equals' = 1)`). `internal/goals/` ships typed Store + CachedStore-free Snapshot (atomic.Pointer hot-swap, SIGHUP reload, NopMatcher for tests). Ingest pipeline stage 7 sets `is_goal=1` + `event_value = goal.value_rials` server-authoritatively (tracker-supplied value overridden; CLAUDE.md Security #3 makes /api/event unsigned). `internal/admin/` ships handlers enforcing F4 via the integration test `internal/httpjson.TestDecodeAllowed_RejectsMassAssignmentAttack`; the `admin-no-raw-json-decoder` Semgrep rule at `.claude/skills/blake3-hmac-identity-review/semgrep/` ships **advisory** (not wired into `make lint`, `make identity-gate`, or any `.github/workflows/*.yml` job in v1 — promote to CI gate in v1.1 alongside the other advisory rules). `internal/httpjson.DecodeAllowed` is the single F4 mass-assignment guard used by both auth + admin (Verification §52). `internal/sites/` extended with `GenerateSlug` + `IsSlugAvailable` + `ReserveSlug` primitives (Phase 11 signup reuses; no HTTP surface in Phase 3c). SPA gets a lazy-loaded `Admin` panel with Users + Goals tabs (role-gated nav entry, +1.66 KB gz). 12 new audit event constants (`admin.*` + `goals.*`). Smoke `probe_admin_flow` covers login → create/list/disable user → create goal → oversized-pattern reject → api-token 403 on admin routes. Unblocks Phase 11 (SaaS signup reuses `httpjson.DecodeAllowed` + `sites.ReserveSlug`). |
| **4 — Tracker JS** | ✅ Complete | PR #21. 1394 B min / 687 B gz + Go embed at `/tracker.js`; `statnive.track()` + `statnive.identify(uid)` end-to-end (raw uid cleared). Anti-automation short-circuits (`webdriver` / `_phantom` / `callPhantom`) fire BEFORE send. **DNT / Sec-GPC moved server-side** in PR D (LEARN.md Lesson 24) — tracker no longer short-circuits on those; binary honors per `consent.respect_*` config (default false). 15 Vitest + 6 Go handler tests; size gate in `make audit`. |
| **5 — Dashboard frontend** | ✅ Complete | Phase 5a (PR #29) + 5b: Preact + signals + TypeScript scaffold, brand tokens CSS, CSP / nosniff / Referrer-Policy on `/app/*`, hash-based zero-dep router, 6 panels (Overview / Sources / Pages / SEO / Campaigns / Realtime) of which 5 dynamic-imported via `LazyPanel` per `bundle-dynamic-imports`, site switcher (`GET /api/sites`) with sessionStorage persistence, IRST date picker + filter panel with URL-hash state sync, uPlot visitors trend on Overview + daily chart on SEO (via `LazyChart` — uPlot stays out of the initial bundle), dual-bar revenue/visitor visualization on Sources/Pages/Campaigns, Realtime panel with 10s polling paused on `document.hidden`. Bundle gate split into five buckets in `web/.size-limit.json` (initial limits bumped in Phase 5e — see that row): initial JS ≤16 KB gz (current 14.70), uPlot chunk ≤25 KB gz (current 23.06), lazy panels ≤10 KB gz, main CSS ≤5 KB gz (current 3.82), per-panel CSS chunks ≤3 KB gz. Backend: `/api/sites` + `/api/stats/trend` endpoints + migration 003 adds `statnive.sites.tz LowCardinality(String) DEFAULT 'Asia/Tehran'`. |
| **5a-smoke — End-to-end boot harness** | ✅ Complete | `test/smoke/harness.sh` + `make smoke` + `smoke-test` CI job drive the real `cmd/statnive-live/main.go` binary against docker-compose ClickHouse and probe every prod surface: `/healthz`, `/tracker.js`, `/app/` (shell + CSP + nosniff + Referrer-Policy + bearer injection), `/app/assets/*.js` (hashed bundle + long-cache), `POST /api/event` (×10 with CH count-back), `GET /api/stats/overview` (401 without bearer; 200 + 5 KPI keys with). Exercises `rateLimitMW` + `BackpressureMiddleware` + `dashboardAuthMW` from the prod router graph on every PR. Canonical pre-cutover verification for Phase 10 (see `docs/runbook.md` § Pre-cutover verification). |
| **5c — Dashboard e2e (Playwright)** | ✅ Complete | `web/e2e/` + `npm --prefix web run e2e` + `dashboard-e2e` CI job drive real Chromium against `bin/statnive-live` + docker-compose CH. Playwright `globalSetup` spawns the binary (mirroring smoke's env-var contract) + seeds 40 synthetic events across 2 sites (801/802) × device/channel/utm dimensions. 30 tests span six feature specs (auth 7, filters 7, navigation 5, panels 6, realtime 3, multitenant 2): auth (bearer + CSP), panels (CH-oracle parity with HLL tolerance), navigation (hash dispatch + browser back/forward via pushState + popstate), filters (chip toggles + URL-hash preservation + custom date range), realtime (visibility-change pauses polling + restore triggers fetch), multitenant (site switcher dropdown + site-switch CH-oracle parity). CH-oracle helper at `web/e2e/fixtures/chOracle.ts` wraps `docker exec clickhouse-client`. Also fixed four 5b bugs surfaced by e2e: `siteSignal` now reads sessionStorage at module load; panels' `useEffect` deps include `siteSignal.value` so they refetch on site change; `navigate()` uses `pushState` + popstate listener so browser back/forward works; handlers return `[]` not `null` for empty list endpoints. Bundle gate unchanged (e2e is devDep). Canonical Phase 10 operator UI-readiness check alongside `make smoke`. |
| **5e — Dashboard visual redesign** | ✅ Complete | Subsequent UX audit at [doc 35](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md) maps a 13-dimension gap matrix against this slice and feeds the new `6-polish-3` / `6-polish-4` / `v1.1-a11y` / `v1.1-frustration-panel` items below. Frontend-only slice. Palette swap (`web/src/tokens.css`) to navy ink on near-white paper, teal primary + amber Admin accent + 7-entry channel palette per the new operator-console brand guideline (`jaan-to/outputs/detect/design/statnive-brand-guideline/statnive-live.html`). **Fonts bundled first-party**: Space Grotesk 500 / DM Sans 400 + 500 / JetBrains Mono 400 + 500 shipped as WOFF2 subsets under `/app/assets/*.woff2` via `@fontsource/*` npm packages (SIL OFL 1.1). Vite `assetsInlineLimit: 0` forces fonts to separate assets so CSP `font-src 'self'` stops blocking them (would otherwise reject inlined `data:` URIs). **Sticky four-row chrome**: new `AppShell.tsx` wraps TopBar → DateBar → Nav → FilterStrip → `<main>`; each row sticks at 0 / 60 / 104 / 152 px on desktop, collapses to static layout under 720 px. **New component primitives**: `DeltaPill` (↑/↓/flat ±1 % deadband, degrades when backend ships no delta — Phase 5f wires the backend), `LivePulse` (8 px green dot + 2 s pulse, respects `prefers-reduced-motion`). **SOON tabs** (Geo / Devices / Funnel) render as `<span role="presentation">` with an "SOON" mono pill — no route, no `PanelName` entry, no click handler; direct `#geo` still falls through to Overview per existing invalid-hash logic. **Admin amber** — Nav tab switches `--active-bar` to `--amber` when `panel === 'admin'` + amber chrome on the Admin sub-tab row. All existing testids (`kpi-primary`, `panel-sources`, `site-select`, `realtime-active` …), `role="tab"` + h2 headings, chip button labels, DatePicker copy ("Last 7 days" / "Custom" / …), `<select>` site switcher, and filter state schema preserved byte-for-byte — every Playwright spec + Vitest test continued to pass unchanged. Net test impact: `tokens.test.ts` updated to 25 new hex values + font-family + fonts.css + no-CDN guards (65 cases); 3 new Vitest specs (`AppShell.test.tsx`, `DeltaPill.test.tsx`, `LivePulse.test.tsx`) + `assets-no-cdn.test.ts` (52 source files scanned); 128 Vitest + 30 Playwright + full Go integration + smoke all green. Bundle gates bumped: initial JS 14 → 16 KB (currently 14.70), CSS stays at 5 KB (currently 3.82), fonts ship as separate ~10–28 KB WOFF2 subsets (outside bundle gate). `LICENSE-third-party.md` added with OFL 1.1 attribution. Zero backend changes. Not on any milestone critical path — lands in parallel to Phase 7d/8 (backend work) on the frontend track. |
| **6 — Config & first-run** | ✅ Complete | Phase 6 shipped the plumbing (YAML loader, migrations, `/healthz`, env override, admin-user via Phase 2b bootstrap, Goal CRUD via Phase 3c). **Phase 6-polish** (first-run UX) ships admin sites CRUD (`POST /api/admin/sites` + list + PATCH enable/disable, reuses `httpjson.DecodeAllowed` + `sites.GenerateSlug/IsSlugAvailable`), SPA Sites tab with per-site tracker-snippet display, SiteSwitcher empty-state CTA for admins, `probe_site_creation` in the smoke harness (replaces the raw SQL seed — full fresh-install-to-first-event flow now exercised in CI), and `docs/quickstart.md` (5-minute copy-paste path). `audit.events.go` + 3 admin.site.* constants. |
| **7a — Backend solidity gate** | ✅ Complete | PR #14. Burst guard (~50 ns/op) + bench suite + crash/CH-outage/disk-full tests + k6 7K EPS + WAL replay + viper env fix. |
| **7c — Optimization & hardening** | ✅ Complete | PR #18. Channel hot path -13% (1 alloc/op); modern Go (`wg.Go`, range-over-int, `b.Loop()`); dead drift-check removed; CI fixes (vendor-check CRLF, license-check GOFLAGS, golangci-lint v2.5 `--new-from-rev`). Audit evidence at `audit/{sec,ch,airgap}-findings.md`; `bench.out` baseline. |
| **7b1a — WAL group-commit foundation** | ✅ Complete | PR #23. `internal/ingest/walgroup.go` GroupSyncer: ack-after-fsync, 256-event batch / 100ms timer, Sync errors terminate via injectable `exitFn` (fsyncgate). |
| **7b1b — WAL integration + perf-test** | ✅ Complete | PR #25. Pipeline synchronous; handler calls `Pipeline.Enrich` + `GroupSyncer.AppendAndWait` before 202. Consumer acks only after CH commit, 100/500/2000ms backoff, 30s drain. `BackpressureMiddleware` → 503 + `Retry-After: 5` at `wal_fill_ratio ≥ 0.80`. WAL replay emits `wal_replay` / `wal_replay_done` + `EventWALCorruptSkipped`. Same-filesystem boot check; LastIndex monotonic guard. `wal-durability-review` body: 4 Semgrep rules + 8 fixtures + 50-iter kill-9 harness. 0.05% loss SLO tightened. Closes items #1,#3,#4,#8,#9,#10 (#2,#5 in PR #23). |
| **7b2 — Real-traffic verification + drills** | ✅ Complete | PR #27. Shipped 5 of 6 deliverables fully + 1 partial: ✅ tracker payload-golden contract (`tracker/test/payload-golden.test.mjs` + `test/tracker_correctness_test.go`); ✅ integration-level PII grep (`test/pii_leak_test.go`); ✅ manual TLS rotation drill (`internal/cert/rotation_e2e_test.go`); ✅ `wal_fsync_p99_ms` on `/healthz` (closes wal-durability-review item #7 — all 10/10 now green); ✅ `make wal-killtest` 5-iter smoke + nightly 50-iter (`wal-killtest-smoke` job + `wal-killtest-nightly.yml`); 🟡 backup-restore drill — manual SOP only (`docs/runbook.md` § Backup & restore); CI automation defers to Phase 2c (needs clickhouse-backup binary + S3 sink). |
| **7b2-completion — execute the 7b2 integration tests in CI** | ✅ Complete | PR #28. New CI jobs: `integration-tests` (docker-compose CH + `make test-integration`) + `tracker-vitest` (Node 20 + `npm --prefix tracker test`). All 8 CI jobs in `ci.yml` green: build-test-lint, licenses, wal-killtest-smoke, integration-tests, tracker-vitest, dashboard-vitest, smoke-test, dashboard-e2e (the latter three joined the workflow in Phases 5a-smoke / 5c / 5e). Also fixed 2 latent Phase 7b2 bugs the wiring exposed: schema column `user_id` → `user_id_hash` in `pii_leak_test.go` + `tracker_correctness_test.go`; missing `HandlerConfig.MasterSecret` in `newTestPipelineServer`. Establishes the "shipped must be executed in CI" rule going forward. |
| **7d — Lint + security scans** | ✅ Complete | PR #TBD. Cleared the **185**-finding baseline on main (not ~40 — `wsl_v5` enable + v2.5.0 bump landed after the original estimate): 87 `wsl_v5`, 18 `intrange`, 15 `errcheck`, 13 `revive`, 10 `gofmt`, 9 `paralleltest`, 7 `gosec`, 6 `usestdlibvars`, 4 `perfsprint`, 3 `staticcheck`. `--new-from-rev=origin/main` flag dropped from `ci.yml`; the full-repo lint is now the hard gate. Three new static gates shipped per [`PLAN.md:89`](PLAN.md#L89): (a) `slog-no-raw-pii` Semgrep rule (F3) + fixtures, wired into `make privacy-gate` and the new `semgrep-identity-privacy` CI job (runs 2 enforced rule sets: `blake3-hmac-identity-review` + `gdpr-code-review`; the other 3 project-local skill rule sets — `geoip-pipeline-review`, `iranian-dc-deploy`, `wal-durability-review` — ship **advisory** per the workflow comments, promotion to hard-gate is tracked as a v1.1 scope item) — research backing at [doc 27 §89](../jaan-to/docs/research/27-closing-three-skill-gaps-statnive-live.md); (b) skill-content sanitizer (F6) — bash + perl scanner for Unicode Tag Block (U+E0000–E007F), zero-width (U+200B–200D, U+FEFF), bidi overrides (U+202A–202E, U+2066–2069) across `.claude/skills/**/*.md` + `docs/**/*.md`, research backing at [doc 25 line 69](../jaan-to/docs/research/25-ai-claude-skills-SamplePlatform-grade-analytics-platform.md#L69); (c) `os.Root` migration (F7) — 2 production `os.Open()` sites (`internal/config/secret.go` master secret, `internal/enrich/newvisitor.go` bloom filter LoadFrom + SaveTo) now resolve via `os.OpenRoot` + `root.Open` for TOCTOU-safe loading (openat2 on Linux / OBJECT_ATTRIBUTES on Windows). Symlink-escape regression tests in both packages. New CI workflow `.github/workflows/security-gate.yml` adds 3 jobs: `govulncheck` (Go supply-chain, v1.1.4), `semgrep-identity-privacy` (2 enforced rule sets — blake3-hmac-identity-review + gdpr-code-review — with SARIF upload to Security tab; geoip / iranian-dc / wal rule sets remain advisory in v1), `codeql` (stock Go + JS + SARIF). All action refs SHA-pinned. SARIF fingerprint baselines (F8) remain deferred to v1.1 per original scope cap. |
| **7e — Load-simulation gate scaffolding** | ⏳ Pending | `test/perf/gate/` Locust harness + k6 CI cross-check + Vegeta/wrk2 breakpoint, 7-scenario chaos matrix (doc 29 §5 + doc 30 §3 international-egress), observability VPS, generator_seq schema migration 003, long-session memory-leak soak (doc 30 §6). Doc 29 §8 W1–W5. HARD GATE on Phase 10 P1 cutover. |
| **8 — Deployment & launch** | ✅ Complete | **Phase 8b GHA release pipeline shipped 2026-04-27.** First GHA-driven release `v0.0.1-rc1` published + auto-deployed to Netcup VPS via `release.yml` → `deploy-saas.yml`. Pre-tag local validation via `make release-fresh` (LEARN.md Lesson 19) is the canonical predictor of `release.yml` outcome. Four deploy-time follow-up gaps tracked at LEARN.md Lessons 20–23 + Verification §63–§65 (one PR `fix/v0.0.1-rc1-deploy-followups` — non-blocking).<br><br>**Phase 8 (foundation, prior PR):** `make airgap-bundle` produces a reproducible `statnive-live-<VER>-linux-amd64-airgap.tar.gz` + `SHA256SUMS` (+ optional Ed25519 `SHA256SUMS.sig` when `SIGNING_KEY=` set). New scripts: `deploy/airgap-bundle.sh` composer, `deploy/airgap-install.sh` idempotent installer (user / dirs / binary / systemd unit / opt-in iptables + first-run bootstrap hints + `--uninstall` keeps data), `deploy/airgap-update-geoip.sh` atomic-mv + SIGHUP + audit-log observe, `deploy/airgap-verify-bundle.sh` SHA256 + Ed25519 verify. GeoIP SIGHUP reload shipped — `internal/enrich/geoip.go` now uses `atomic.Pointer[ip2location.DB]` + pre-swap probes (`8.8.8.8` → non-empty country; `185.143.232.1` → IR); failed probes leave the old handle active. File-sink alerts shipped at `/var/log/statnive-live/alerts.jsonl` (`internal/alerts/` — sink.go + band.go + prober.go) covering 4 conditions with enter/exit-band debouncing: WAL fill (0.80 / 0.90 / 0.95), CH Ping up/down, disk fill on `/var/lib/statnive-live` (0.85 / 0.90 / 0.95), TLS expiry (<30d warn / <7d critical). `docs/runbook.md` adds § Air-gap bundle install + § GeoIP update procedure + § Alerts file format + § Log rotation. Staging install verified via `docker run ubuntu:24.04`: install → unit installed → uninstall → binary + unit removed, config + data retained. **Still pending (out-of-scope for Phase 8):** Netcup VPS 2000 G12 NUE real traffic (Phase 9 — primary D1 procured 2026-04-24 per doc 36 fallback because Hetzner doc-verification is pending; Hetzner CX43/AX41 remains the future Phase C growth-tier upgrade when that account activates), SamplePlatform Iranian-DC provisioning (Phase 10), Docker tarball (v1.1), Telegram / syslog remote-alert fan-out (v1.1), Ed25519 signing CLI (v1.1 / Phase 11b), alerts dashboard UI (6-polish-5). |
| **9 — Phase A dogfood** | ✅ Done | **v0.0.1-rc1 GHA release shipped 2026-04-27 13:14 CEST** — first end-to-end GHA-driven release. GitHub Release at https://github.com/statnive/statnive.live/releases/tag/v0.0.1-rc1 with 3 signed assets (`*.tar.gz` + `SHA256SUMS` + `.sig`); VPS healthz green post-deploy. Backed by 8 fix PRs in this session (#64 / #66 / #69 / #70 / #71 / #72 / #73 / #74) plus the strategic shift to `make release-fresh` local validation captured as LEARN.md Lesson 19.<br><br>**Initial cutover:** Cutover executed **2026-04-25** on Netcup VPS 2000 G12 NUE (Debian 13, `94.16.108.78` / `2a03:4000:51:f0c::1`). 24 h soak passed **2026-04-26** at T+24h: `/healthz` returned `clickhouse=up`, `wal_fill_ratio=0.0017`, `wal_fsync_p99_ms=14.9`, `last_insert` recent — all within SLO. Live state: TLS 3-SAN cert (statnive.live + app + demo) valid until 2026-07-24; Cloudflare DNS-only mode for `.live`; ClickHouse 26.5.1.68 running; binary listening on `0.0.0.0:443` with TLS; 3 sites seeded (statnive.com / .de / fr.statnive.com). All four cutover PRs shipped (#46, #47, #48, statnive-website tracker swap) plus the **6-PR follow-up series** closing the 24 cutover bugs: prereq license fix (#54), schema parity (#55, PR-B), install perms + Debian 13 matrix (#56, PR-C), bundle completeness + cross-compile + version regex (#57, PR-A), cutover-script hardening (#58, PR-D), tracker fallback + cookie doc (#59, PR-E), three-flag consent posture (#61, PR-E2 — Option C). Full bug catalog in [§ Milestone 1 cutover postmortem (2026-04-25)](#milestone-1-cutover-postmortem-2026-04-25). **Commercial gate** — [§ Phase 11a-pre: GDPR ops gate](#phase-11a-pre-gdpr-ops-gate-hard-gate-on-first-paying-saas-customer) (Netcup DPA, sub-processor register, /privacy page, MX null + SPF + DMARC + DNSSEC, LUKS, NTP, HSTS, swappiness, **counsel review**) blocks first paying SaaS customer; runs as an independent ops track and does NOT block Milestone 1 functional close. |
| **10 — Phase B SamplePlatform** | ⏳ Pending | Iranian DC bare metal, paid DB23 GeoIP. |
| **11a — SaaS self-serve signup (free tier)** | ⏳ Pending | 🎯 **Milestone 3**. `POST /api/signup` (email + password + hostname → site + admin user), DNS-resolvable check, 5/hr/IP rate-limit, email verification (24 h grace), path-based tenant routing `app.statnive.live/s/<slug>/`, onboarding page, free 10K PV/mo. **No billing.** Reuses `httpjson.DecodeAllowed` + `sites.ReserveSlug` from Phase 3c. |
| **11b — SaaS paid tiers (Polar.sh)** | ⏳ Pending | 🎯 **Milestone 4**. `POST /api/billing/checkout` → Polar, `POST /api/admin/billing` (Polar webhook; HMAC signature verified), 4 Products × monthly+yearly ($9 / $19 / $69 / $199), plan-gated features (Funnel CRUD, higher quotas), upgrade-banner on over-quota. Polar is Merchant of Record — no per-country tax registration. Customer Portal + Benefits = v2. |
| **v1.1-tokens — API-token rotation endpoint** | ⏳ Pending | `POST /api/admin/tokens` (hash + rotate without binary restart). **Blocks 11a** (SaaS multi-tenancy requires leaked-token rotation without restart). Can fold into the 11a PR or ship standalone. |
| **v1.1-pwreset — Password-reset email flow** | ⏳ Pending | Forgot-password link → signed token → reset landing page → `POST /api/auth/password-reset`. **Blocks 11a** (customers forget passwords; no self-serve product without it). NIST 800-63B password policy ships in this slice alongside Phase 11a's signup validation. |
| **v1.1-csrf — CSRF double-submit + step-up auth** | ⏳ Pending | Double-submit token on `/api/admin/*` + `RequireFreshAuth(5 m)` on privilege-granting handlers (password change, role change, billing change). **Blocks 11b** (paid multi-tenant surface raises the value of a leaked admin cookie). |
| **v1.1-ci-drill — Re-enable nightly backup-drill CI** | ⏳ Pending | `backup-drill-nightly.yml` was demoted to `workflow_dispatch`-only in Phase 2c (PR #41) because `clickhouse/clickhouse-server:24.12-alpine` Atomic-engine gates DROP on the `force_drop_table` flag for MV inner-tables regardless of `max_table_size_to_drop`. Host-side operator cron remains the release-blocking SoT (`docs/runbook.md` § Backup & restore). Re-enable when (a) CH point release drops the flag requirement, (b) a continuous-flag-touch workaround passes `/simplify`, or (c) a different CH image works. |
| **6-polish-3 — Empty / loading / error state primitives** | ⏳ Pending | `EmptyState` / `LoadingState` / `ErrorState` components per [doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §5 R2](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). ~1 day; folds into the `6-polish-2` PR. |
| **6-polish-4 — Color-not-only error/success carrier** | ⏳ Pending | Icon prefix on `role="alert"` rows per [doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §5 R3](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). ~1 hour; folds into `6-polish-3` PR. |
| **6-polish-5 — Notice / toast / status primitive** | ⏳ Pending | Single ~3 KB hand-rolled `Notice.tsx` covering toast (auto-dismiss async outcomes), persistent non-fatal warnings (GeoIP fail / cron disabled / retention behind), and `role="status"` transient state ("Refreshing…"). Per [doc 35 §2.7 / §2.8 / §5 R9](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). ~1 day. |
| **6-polish-6 — Role chip in TopBar** | ⏳ Pending | Explicit "admin" / "viewer" / "api" pill next to the user-email element so a viewer who doesn't see the Admin tab understands why. Per [doc 35 §2.10](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). ~30 min. |
| **6-polish-7 — Resolved-visitors UI scaffold** | ⏳ Pending | Row pill ("anonymous" / "identified") on Sources / Pages + FilterStrip filter chip. Per [doc 35 §2.10](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). ~1 day; **gated on the Resolved Visitors data layer landing** (currently v2). Tracked now so UI ships in lockstep with the ledger. |
| **v1.1-a11y — WCAG 2.2 AA gate + chart a11y + motion-reduction** | ⏳ Pending | Four deliverables: `@axe-core/playwright` zero-violation gate + hidden `<table>` mirror on every uPlot chart + keyboard-trap audit against the four-row sticky `AppShell.tsx` + extend `prefers-reduced-motion` honoring beyond `LivePulse` to all transitions (sparkline, modal, toast, DeltaPill flash). Per [doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §5 R13 + §2.8](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). **Optional prereq to 11a** (internet-facing dashboard = enterprise/legal liability). Recommend folding into 11a's PR. |
| **v1.1-frustration-panel — Health panel for the Safe Autocapture Pack** | ⏳ Pending | New lazy `web/src/panels/Health.tsx` rendering rage clicks (>3/sec), dead clicks, p75 INP / LCP, Long Task counts per page path. Pairs with the v1.1 `pack-safe.js` Safe Autocapture Pack (CLAUDE.md §"Deliberate skips"). The deliberate substitute for session replay. Per [doc 21](../jaan-to/docs/research/21-microsoft-clarity-codebase-review-patterns-for-self-hosted-analytics.md) + [doc 32 §A](../jaan-to/docs/research/32-posthog-statnive-deep-research.md) + [doc 35 §5 R7](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md). Optional prereq to Phase 10 P1 cutover. |
| **CLI** (operator surface) | 🔮 v1.1 | Subcommands: serve, migrate, license, sites, users, backup, doctor, secret, stats. Details in [`docs/cli-operator-surface.md`](docs/cli-operator-surface.md). |
| **MCP server** (agent surface) | 🔮 v2 | Read-only analytics tools over stdio (air-gap-safe) or HTTP. See [`docs/cli-operator-surface.md § MCP`](docs/cli-operator-surface.md). |
| **Brand & design tokens** | 📐 Reference ready | Wordmark + summit logo, cream/ink/Persian-Teal palette, Fraunces + IBM Plex ramp. Full spec in [`docs/brand.md`](docs/brand.md). |

### Shipping Roadmap — 4 milestones (2026-04-23)

After Phase 3c merged, the remaining v1 work is sequenced to ship **four product milestones in order**. Each milestone is a customer-visible checkpoint; intermediate phases are foundation.

| # | Phase | Effort | Cumulative | Ships when done |
|---|---|---|---|---|
| 1 | ✅ **2c — Operational hardening** | 2 days | Day 2 | systemd unit, firewall rules, backup script, LUKS docs (shipped PR #36) |
| 2 | ✅ **6-polish — First-run UX** | 1 day | Day 3 | Fresh install → first event visible <5 min (shipped PR #42) |
| 3 | ✅ **7d — Lint + security scans** | 3 days | Week 1 | govulncheck + CodeQL + Semgrep in every PR + 185 pre-existing findings cleared (shipped PR #TBD) |
| 4 | ✅ **8 — Deploy + airgap bundle** | 1 week | Week 2 | `make airgap-bundle` → SCP → install → v1 binary running (shipped PR #TBD) |
| 5 | ✅ **9 — Dogfood on statnive.com / .de / fr.statnive.com** | 2 days | Week 2 | 🎯 **Milestone 1: statnive.com live on Statnive — DONE 2026-04-26** — cutover 2026-04-25 + 24h soak passed at T+24h. 4 cutover PRs (#46/#47/#48 + statnive-website tracker swap with .de/.fr mirror sync) + 6 follow-up PRs (#54 prereq licenses, #55 PR-B schema parity, #56 PR-C install perms + Debian 13, #57 PR-A bundle completeness + cross-compile, #58 PR-D cutover-script hardening, #59 PR-E tracker fallback, #61 PR-E2 three-flag consent posture). 24 bugs cataloged + closed — see [§ Milestone 1 cutover postmortem](#milestone-1-cutover-postmortem-2026-04-25). Commercial gate (GDPR ops + counsel review) tracked separately at [§ Phase 11a-pre](#phase-11a-pre-gdpr-ops-gate-hard-gate-on-first-paying-saas-customer); blocks first paying SaaS customer, NOT Milestone 1. |
| 6 | **7e — Load-gate + 7-scenario chaos matrix** | 4 weeks | Week 6 | 72 h soak @ 7K EPS + full chaos matrix green (HARD GATE on M2) |
| 7 | **10 P1 — SamplePlatform cutover (web only)** | 4 weeks | Week 10 | 🎯 **Milestone 2: SamplePlatform live (Iranian DC)** |
| 8 | **v1.1-tokens — API-token rotation endpoint** | 3 days | Week 10.5 | `POST /api/admin/tokens` — rotation without binary restart |
| 9 | **v1.1-pwreset — Password-reset email flow** | 1 week | Week 11.5 | Forgot-password → email → reset landing page |
| 10 | **11a — SaaS self-serve signup (free tier)** | 2 weeks | Week 13.5 | 🎯 **Milestone 3: public signup, 10K PV/mo free, no billing** |
| 11 | **v1.1-csrf — CSRF double-submit + step-up auth** | 1 week | Week 14.5 | Admin mutations hardened for multi-tenant paid surface |
| 12 | **11b — Polar.sh checkout + webhooks + plan gating** | 3 weeks | Week 17.5 | 🎯 **Milestone 4: paying SaaS customers** |
| 13 | **flags — Statnive Flags (local-first feature flags)** | 3 weeks | Week 20.5 | 🎯 **Milestone 5: local-eval flags for self-hosted + SaaS** |

**Ongoing after Milestone 2.** Phase 10 P2–P5 (SamplePlatform iOS / Android / TV rollout) runs over ~10 months in the background, NOT on Milestone 4's critical path. Each sub-phase repeats the 7e load-gate at a larger EPS envelope (P1 450 EPS → P5 40K EPS peak) before onboarding the next app.

**Parallelization.** After Phase 8 (deploy bundle) lands at Week 2, three tracks can run independently if two developers are available:

- **Track A** — `9 → 7e → 10 P1` → Milestone 2 at Week 10
- **Track B** — `v1.1-tokens + v1.1-pwreset → 11a` → Milestone 3 at Week 8 (overlaps M2 timeline)
- **Track C** — `v1.1-csrf → 11b` → Milestone 4 at Week 13 (overlaps too)

Two-developer execution compresses the 17.5-week sequential estimate to ~13 weeks (~6 weeks saved). Solo execution = strictly sequential per the critical-path table. Caveat: Track B uses Hetzner staging, not the Iranian DC, so Track A's 7e load-gate failure does not block Track B.

**Decision caveats to revisit before starting each milestone:**

- **M2 — SamplePlatform contract timing.** If SamplePlatform has a fixed go-live date, Phase 10 P1 Week 10 becomes a hard deadline. Confirm with product before starting 7e.
- **M3 — email verification lives inside 11a**, not deferred to v1.1. Phase 11a without email verification is spammable; add the ~3-day email-verification scope to the Phase 11a week estimate.
- **M4 — Polar lock-in.** Switching to Stripe after shipping 11b = ~1 week of rework. Confirm Polar is the right choice (MoR trade-off vs. fee, EU VAT coverage) before starting 11b.
- **M4 — feature-gating philosophy.** "Paid tiers unlock goals/funnels" is the default in the original plan. Recommend: leave Goals (Phase 3c) free across all tiers; gate only the eventual Funnel CRUD (v2) + higher quotas behind paid. Keeps Milestone 3's upgrade story simple ("more usage = upgrade", not "more features = upgrade").
- **v1.1 fold-or-standalone.** v1.1-tokens / -pwreset / -csrf can fold into their blocking milestone's PR (less ceremony) or ship as three small standalone PRs (easier review). Decide per slice at execution time.

### Phase 0: Project Setup (Week 1)

**Guardrail:** [`air-gap-validator`](.claude/skills/air-gap-validator/README.md).

- [x] Repo, Go module, Makefile (build/test/lint/release/`airgap-bundle`), CI (SHA-pinned actions)
- [x] ClickHouse schema SQL (`events_raw` + 3 v1 rollups)
- [x] Copy Go files from doc 22; vendor all deps
- [x] `config/sources.yaml` (64 entries); `config/statnive-live.yaml`

### Phase 1: Ingestion Pipeline (Weeks 2–4)

**Guardrails:** [`tenancy-choke-point-enforcer`](.claude/skills/tenancy-choke-point-enforcer/README.md), [`blake3-hmac-identity-review`](.claude/skills/blake3-hmac-identity-review/README.md), [`clickhouse-rollup-correctness`](.claude/skills/clickhouse-rollup-correctness/README.md), [`clickhouse-cluster-migration`](.claude/skills/clickhouse-cluster-migration/README.md), [`wal-durability-review`](.claude/skills/wal-durability-review/README.md).

- [x] Wire main.go; SiteID on EnrichedEvent
- [x] `ingest/handler.go` — JSON array parsing; pre-pipeline fast-reject (prefetch headers, UA shape, IP-as-UA, UUID-as-UA, non-ASCII → 204). Parse `True-Client-IP` + `CF-Connecting-IP` alongside XFF rightmost.
- [x] `ingest/pipeline.go` — 6-worker enrichment; order locked: identity → burst → bloom → geo → ua → bot → channel. Cheap-first bot detection. Burst guard in PR #14. v1.1 owes referrer-spam + browser-version-floor + datacenter-CIDR.
- [x] `ingest/consumer.go` — dual-trigger batch writer (1000 rows / 500ms / 10MB). Single 250ms retry; DLQ deferred.
- [x] `ingest/wal.go` — 100ms fsync + 10GB cap; `/healthz` `wal_fill_ratio`.
- [x] `storage/clickhouse.go` — 34-column batch insert + site_id; `DateTime('UTC')` not `DateTime64(3)`.
- [x] `storage/migrate.go` — numbered migrations, `schema_migrations(version, dirty, sequence)`, `{{if .Cluster}}` templates from day 1. Advisory locks deferred.
- [x] `enrich/` — GeoIP (LITE DB23 no-op fallback), medama-io UA, 17-step channel tree, isbot + `crawler-user-agents.json`, 18MB bloom. Hostnames via `map[string]struct{}` (~100× speedup).
- [x] `identity/` — BLAKE3-128 + `HMAC(master_secret, site_id || YYYY-MM-DD IRST)`. Cross-day fingerprint grace lookup.
- [ ] k6 load test (Phase 7): P1 ~300 / P2 ~1,000 / P3 ~4,000 / P4 ~9,000 (18K match) / P5 ~40,000 peak EPS per doc 29 §4 — the design-target graduation-gate sign-off numbers. Doc 30 §4 observed current-state is ~4× lower (P5 ~8K sustained, 12K burst) and is retained as a **realism overlay for load-shape curve fitting only**, not a target override (see Context "design ceiling vs. observed"). Smoke only in CI; per-phase graduation gate runs in Phase 7e against the design target.
- [ ] Crash recovery test (Phase 7): kill-9 → WAL replay → zero loss.
- [x] Integration tests: bot UA → is_bot=1 + visitor_hash (`test/enrichment_e2e_test.go`); 10-case fast-reject table; cross-day grace.

### Phase 2: Security Layer (Weeks 5–6)

**Guardrail:** [`ratelimit-tuning-review`](.claude/skills/ratelimit-tuning-review/README.md) — regression guard for Phase 2a rate-limiter. ASN tiering + `iptoasn.com` is Phase 10 HARD GATE.

- [x] TLS manual PEM loader + SIGHUP reload (`internal/cert/`, atomic.Pointer hot-reload, fail-closed, keep-old-on-fail, expiry watcher <30d/<7d).
- [ ] Dashboard auth — Phase 2b.
- [x] Rate limiting (`httprate.LimitByRealIP`, 100 req/s, NAT-aware via `ingest.ClientIP` ladder; emits `audit.ratelimit.exceeded`).
- [x] Input validation (`MaxBytesReader` 8KB, ±1h drift) — shipped in Phase 1.
- [x] Hostname validation (HMAC skipped per doc 20); emits `audit.ingest.hostname_unknown`.
- [x] Audit log (`internal/audit/`, `O_APPEND`, `Reopen()` for logrotate, typed `EventName`).
- [x] systemd hardening, iptables, LUKS docs, backup script — Phase 2c (PR #36). `deploy/systemd/`, `deploy/iptables/`, `docs/luks.md`, `deploy/backup/drill.sh` + `.github/workflows/backup-drill-nightly.yml` (Altinity clickhouse-backup sidecar + MinIO round-trip).
- [x] Security assertions in `test/integration_test.go` + `test/security_test.go`.

### Phase 3: Dashboard API (Weeks 7–9)

**Guardrail:** [`tenancy-choke-point-enforcer`](.claude/skills/tenancy-choke-point-enforcer/README.md) — pairs with existing `make lint` `tenancy-grep`.

Flat `internal/storage/queries.go` (one Go function per endpoint) — we do NOT mirror Pirsch's 10 sub-analyzer split (doc 24 §Sec 4 pattern 1).

- [x] `storage/store.go` — typed Store interface; one method per endpoint; mockable for Phase 7.
- [x] `storage/queries.go` — central `whereTimeAndTenant(*Filter, col)` helper; CI `tenancy-grep` rejects any `SELECT` skipping it.
- [x] `storage/filter.go` — Filter struct (SiteID, From/To, Path, Referrer, UTM, Country, Browser, OS, Device, Sort, Search). Deterministic BLAKE3 `Hash()` as cache key.
- [x] 8 `GET /api/stats/*` handlers (overview/sources/pages/geo/devices/funnel/campaigns/seo). Geo/Devices/Funnel return 501 until v1.1/v2.
- [x] `WITH FILL … STEP INTERVAL` for SEO daily series. Visitors-trend deferred.
- [ ] Admin CRUD — Phase 3c (needs Phase 2b).
- [x] `GET /api/realtime/visitors` (10s cache).
- [x] Half-open `[from, to)` intervals at day granularity; Asia/Tehran conversion at API layer.
- [x] LRU cache (realtime=10s, today=60s, yesterday=1h, historical=∞) — `internal/cache/{lru,policy}.go`; per-entry TTL via `expiresAt`.
- [ ] Funnel via `windowFunnel()` + 1h cache — v2.
- [ ] Dashboard query benchmark under 7K EPS — Phase 7.
- [x] Multi-tenant integration test (`test/dashboard_isolation_test.go`).

### Phase 4: Tracker JS (Week 10)

**Guardrails:** [`preact-signals-bundle-budget`](.claude/skills/preact-signals-bundle-budget/README.md) (1.2KB min / 600B gz tracker budget + CDN ban); [`air-gap-validator`](.claude/skills/air-gap-validator/README.md).

- [x] Build from doc 20 — 1394 B min / 687 B gz; Rollup + Terser passes=3 + mangle.toplevel; output to `internal/tracker/dist/`.
- [x] Pageview + SPA (history API) + custom events (`statnive.track(name, props, value)`) + `statnive.identify(uid)` (raw cleared in handler, hashed via `identity.HexUserIDHash`).
- [x] Client bot hints: `navigator.webdriver`, `_phantom`, `callPhantom` short-circuit before send.
- [x] **Sec-GPC + DNT short-circuit BEFORE the request fires** (doc 27 Privacy Rule #9).
- [x] Served via `go:embed` (`internal/tracker/tracker.go`).
- [x] Cross-day returning visitors validated via `_statnive` cookie round-trip.
- [x] Real-tracker integration test → rollup verification — Phase 7b2 shipped the payload-golden contract (Vitest captures sendBeacon body in `tracker/test/payload-golden.test.mjs`; Go integration test replays each payload through the full pipeline → ClickHouse in `test/tracker_correctness_test.go`). Phase 7b2-completion (PR #28) wires both into CI — `tracker-vitest` job runs the Vitest; `integration-tests` job runs the Go replay against docker-compose ClickHouse.

**Deferred to v1.1 — Safe Autocapture Pack** (doc 31 §"Safe defaults" / doc 32 §A / doc 34 §E). Separate `tracker/pack-safe.js` module, ~1 KB gz, opt-in per `<script data-pack="safe">` or `statnive.enablePack('safe')` — keeps base tracker at ~687 B gz. Adds `$pageleave`, Core Web Vitals (LCP/FID/CLS/FCP), scroll-depth buckets (25/50/75/100), rage clicks (>3 clicks in 1 s), outbound-link clicks, file-downloads, form-submit success/failure. The pack inherits the same anti-automation short-circuit as the base tracker; `Sec-GPC: 1` / `DNT: 1` are forwarded to the server which applies operator-configured policy (`consent.respect_*`, see Privacy Rule 9). Adds one column `event_type Enum8(...)` to `events_raw` via migration 007. Excludes element text, form values, DOM snapshots, canvas/WebGL/font-enum, device-memory — already banned by Privacy Rule 7.

**Deferred further to v2:** engagement ping (10 s heartbeat), throttle-with-last-event, base36 date, envelope+payload separation.

### Phase 5: Dashboard Frontend (Weeks 11–13)

**Guardrails:** [`preact-signals-bundle-budget`](.claude/skills/preact-signals-bundle-budget/README.md) (50KB min / 15KB gz); plus `vercel-labs/web-design-guidelines` + `knip-unused-code-dependency-finder`.

Brand tokens from [`docs/brand.md`](docs/brand.md) — `web/src/tokens.css` imports at SPA entry; hand-rolled hex in components is a PR-review reject.

- [x] Preact SPA scaffold (Vite + TypeScript + @preact/signals) — Phase 5a / PR #29.
- [partial] Overview panel done (Phase 5a); Sources / Pages / SEO / Campaigns / Realtime panels + uPlot Visitors trend pending Phase 5b; Funnel (Frappe) / Geo / Devices pending v1.1 or v2 per rollup availability.
- [ ] Gregorian date picker; real-time widget (10s refresh); Admin pages (users/goals/funnels) — Phase 5b (non-admin) + Phase 2b+3c (admin pages).
- [x] WCAG 2.2 AA contrast + ARIA + visible labels + role attributes shipped in Phase 5c. Automated `axe-core` gate, hidden `<table>` mirror on uPlot charts, and the keyboard-trap audit against the Phase 5e four-row sticky `AppShell.tsx` are deferred to **`v1.1-a11y`** (see v1.1 list) per [doc 35 §5 R13](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md).
- [x] Embed via go:embed — Phase 5a. Binary-size verification deferred until Phase 5b panels land.

**Deferred to v1.1:** comparison-period toggle, CSV export, command palette.

### Phase 6: Configuration & First-Run (Week 15) — Polish for Milestone 1

- [x] YAML config loader (viper). Goals storage flipped CH-backed in Phase 3c (see Phase 6 note); funnels stay deferred to v2.
- [x] First-run: admin user creation (Phase 2b bootstrap env) + Goal CRUD (Phase 3c CH-backed, not YAML).
- [x] Schema migration runner; `/healthz`
- [x] **Phase 6-polish (PR #42)** — admin sites CRUD (`POST /api/admin/sites` + `GET /api/admin/sites` + `PATCH /api/admin/sites/{id}`) extends the Phase 3c admin surface; `sites.Registry` gains `CreateSite`/`ListAdmin`/`UpdateSiteEnabled` on top of the existing `GenerateSlug`/`IsSlugAvailable`/`ReserveSlug` primitives.
- [x] **Phase 6-polish (PR #42)** — SPA Admin panel gets a third Sites tab (lazy-loaded alongside Users + Goals) with a per-row tracker snippet `<pre>` block rendered from `window.location.origin`. `SiteSwitcher.tsx` empty-state becomes a `no sites yet — add one` nav link for admins.
- [x] **Phase 6-polish (PR #42)** — `docs/quickstart.md` is the 5-minute copy-paste path: clone → openssl master-secret → docker compose CH → boot binary → Admin → Sites → paste snippet → first event. `test/smoke/harness.sh` replaces its raw `INSERT INTO statnive.sites` seed with a `probe_site_creation` that exercises `/api/admin/sites` end-to-end; the smoke gate now validates the true fresh-install path on every PR.
- [ ] **Phase 6-polish-2 — Literal wait-for-first-event gate on Overview** (doc 33 §6 / doc 34 §A / [doc 35 §5 R1](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); folds into any near-term frontend PR, ~1 day). When `siteOverview.totalVisitors === 0 && siteCreatedAt < 24 h`, render `<InstallVerifyCard>` in place of the Overview panel. Polls `/api/stats/overview` every 5 s, auto-flips on first event. Install snippet inline with copy-button + `?debug=1` tracker-verify link. PostHog's lesson: an empty dashboard teaches "this tool doesn't work"; a "waiting" state teaches "this tool works, I just haven't finished setup." Zero backend change; Preact conditional in `web/src/panels/Overview.tsx`. Phase 5c e2e gains `install-verify.spec.ts`.
- [ ] **Phase 6-polish-3 — Empty / loading / error state primitives** ([doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §2.2 / §5 R2](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~1 day, folds into the same PR as 6-polish-2). New `EmptyState`, `LoadingState`, `ErrorState` components in `web/src/components/` replace the bare `<p class="statnive-loading">loading…</p>` / `<p class="statnive-error">could not load — see logs</p>` pattern at `web/src/panels/panels.css:157-162`. Each state explains *why* (CSP blocked, consent declined, endpoint unreachable, salt rotated, time range empty, filter excludes all rows) and offers a next-action affordance ("Clear filters" / "Retry now" / link to `docs/quickstart.md`). Distinguishes "no data in range" from "filter excludes all rows" with an explicit Clear-filters button on the latter. Touches all 7 panels for the swap. Bundle: ~0.5 KB gz net add (well under the 16 KB gz initial gate).
- [ ] **Phase 6-polish-4 — Color-not-only error/success carrier** ([doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §5 R3](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~1 hour, folds into 6-polish-3 PR). Add icon prefix glyph (`✕` for error, `✓` for success) to `role="alert"` rows so error vs success differs in shape, not only hue — current `var(--error) #B0243A` vs `var(--green) #00A693` differ only in hue (WCAG SC 1.4.1). Touches the shared notice primitive (or inline `role="alert"` rows in `web/src/pages/Login.tsx` + `web/src/panels/Admin.tsx`). Bundle: ~0.1 KB gz.
- [ ] **Phase 6-polish-5 — Notice / toast / status primitive** ([doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §2.7 / §2.8 / §5 R9](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~1 day; bundles three S/M-impact gaps from doc 35 into one ~3 KB hand-rolled component). Single `web/src/components/Notice.tsx` (mounted in `AppShell`) using `@preact/signals` for the queue, with three modes: (a) **toast** — auto-dismiss after 4 s, manual close, two variants info / error, `role="status"` for info and `role="alert"` for error, for async outcomes (cron task done, backup complete, GeoIP DB updated, password-reset email sent); (b) **persistent notice** — non-fatal warnings (GeoIP download fail, cron disabled, retention behind by N hours), dismissible with explicit "Retry" action and explanation of consequence; (c) **status line** — transient "Refreshing…" indicator for in-flight queries, `role="status"` so screen-readers announce without stealing focus (WCAG 2.2 §4.1.3). Avoid `react-hot-toast` / `sonner` — React-only, 6-12 KB, breaks bundle gate. Honors `prefers-reduced-motion` for slide-in. Bundle: ~3 KB gz; well under the 16 KB initial-JS gate.
- [ ] **Phase 6-polish-6 — Role chip in TopBar** ([doc 35 §2.10](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md) + design brief §Roles; ~30 min). Add an explicit "admin" / "viewer" / "api" pill next to the user-email element in `AppShell.tsx`'s TopBar so a viewer who doesn't see the Admin tab understands why. Already implicit (Admin tab is hidden for non-admins) but making it explicit closes the "where did the Admin tab go?" support ticket. Reuses `var(--green-lt)` chip styling. Bundle: ~0.2 KB gz.
- [ ] **Phase 6-polish-7 — Resolved-visitors UI scaffold** ([doc 35 §2.10 / §3](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~1 day; gated on the Resolved Visitors data layer landing — currently v2). Once the resolved-visitors ledger ships (links anonymous `visitor_hash` ↔ identified `user_id_hash` with merge rules per [doc 32 §A](../jaan-to/docs/research/32-posthog-statnive-deep-research.md)): one row pill on Sources / Pages tables ("anonymous" var(--ink-2) / "identified" var(--green-dk), never the raw uid — only the hash) + one FilterStrip chip ("show identified only / anonymous only / both"). Tracking now so the UI scope ships in lockstep with the ledger; do not build until the ledger is in flight.

### Phase 7: Testing & Hardening (Week 16)

**Guardrails:** [`air-gap-validator`](.claude/skills/air-gap-validator/README.md) (release-gate under `iptables -P OUTPUT DROP`); [`wal-durability-review`](.claude/skills/wal-durability-review/README.md) — **hard gate on 7b close** (kill-9 CI 50 runs/PR).

- [x] k6 smoke (`test/perf/load.js`, PR #14, Persian paths + Iranian UAs, `make load-test`)
- [x] Go bench suite (`internal/{ingest,enrich}/bench_test.go`, PR #14)
- [ ] 100K-event integration (Phase 7b, after auth)
- [x] Crash recovery test (logs ~80% loss before 7b WAL fix — now asserts <0.05%)
- [x] Disk-full policy test; Phase 7c optimization (PR #18 — Channel -13%, modern Go, CI fixes, `make audit`)
- [x] Backup restore — Phase 7b2 ships the manual SOP at [`docs/runbook.md`](docs/runbook.md) § Backup & restore; Phase 2c (PR #36) automates it via [`deploy/backup/drill.sh`](deploy/backup/drill.sh) + [`.github/workflows/backup-drill-nightly.yml`](.github/workflows/backup-drill-nightly.yml) (Altinity clickhouse-backup + MinIO sidecar, nightly round-trip with row-count parity + `countMerge(visitors_hll_state) FINAL` mergeability check).
- [x] Manual TLS rotation test — Phase 7b2 ([`internal/cert/rotation_e2e_test.go`](internal/cert/rotation_e2e_test.go) — atomic.Pointer hot-swap regression + fail-closed on corrupt PEM).
- [x] CH outage buffer-and-drain test (10s in-test; 10min in runbook)
- [x] Integration-level PII grep — Phase 7b2 shipped [`test/pii_leak_test.go`](test/pii_leak_test.go) (byte-scans WAL segments + audit.jsonl + `events_raw` for raw user_id/IP probes; pins Privacy Rules 1 + 4); Phase 7b2-completion (PR #28) executes it per PR via the new `integration-tests` job. Latent `user_id` vs `user_id_hash` column drift fixed in the same PR.
- [x] WAL fsync p99 surfaced via `/healthz` — Phase 7b2 closes [`wal-durability-review`](.claude/skills/wal-durability-review/README.md) item 7 (last open of 10).
- [x] Kill-9 WAL CI gate — Phase 7b1b shipped harness; Phase 7b2 wires `make wal-killtest` 5-iter smoke into per-PR CI + nightly 50-iter on main.
- [x] **Lint baseline + static + supply-chain gates** — Phase 7d (PR #TBD) cleared the 185-finding `golangci-lint` baseline on main, dropped `--new-from-rev=origin/main` from `ci.yml` (full-repo lint is now the hard gate), shipped `slog-no-raw-pii` Semgrep rule (F3, research doc 27 §89) with fixtures + `make privacy-gate`, shipped `scripts/skill-sanitizer.sh` (F6, research doc 25 line 69) scanning `.claude/skills/**` for Unicode Tag Block / zero-width / bidi codepoints with fixtures + `make skill-sanitizer`, and migrated 2 production `os.Open()` call sites (`internal/config/secret.go`, `internal/enrich/newvisitor.go`) to `os.OpenRoot` + `root.Open` (F7, Go 1.24 TOCTOU-safe loader) with symlink-escape regression tests. New CI workflow `.github/workflows/security-gate.yml` adds `govulncheck` + `semgrep-all` (all 5 project-local skill rule sets, SARIF upload) + `codeql` (stock Go + JS, SARIF upload) jobs per PR. SARIF fingerprint baselines (F8) deferred to v1.1 per scope cap.
- [ ] **Generator_seq oracle schema** (doc 29 §6.1) — migration `003_load_gate_columns.sql` adds `test_run_id` (UUID), `test_generator_seq` (UInt64), `generator_node_id` (UInt16), `send_ts` (DateTime64(3)) to `events_raw` with typed `DEFAULT` sentinels (not Nullable — Rule 5 carve-out in CLAUDE.md); projection `proj_oracle` for sub-second per-run aggregations. Phase 7e prerequisite — scaffolded alongside Locust harness.
- [ ] **PII wire-scan migration to Vector.dev + VRL** (doc 29 §3.4, §6.3) — supersedes one-shot [`test/pii_leak_test.go`](test/pii_leak_test.go) with live <1s detection at 15K+ EPS via VRL regex (ipv4/ipv6/email/user_id). Halts graduation gate on `rate() > 0`. Phase 7e deliverable.

### Phase 7e: Load-simulation gate scaffolding (Weeks 17–20, overlaps Phase 8) — Hard gate for Milestone 2

**Guardrail (scheduled):** `load-gate-harness` skill — triggers on `test/perf/gate/**`, `test/perf/chaos/**`, `test/perf/generator/**`, `deploy/observability/**`. Advisory during scaffolding; HARD GATE on Phase 10 P1 cutover. Canonical spec: [`../jaan-to/docs/research/29-Production-load-simulation-gate-statnive-live-asiatech-tehran.md`](../jaan-to/docs/research/29-Production-load-simulation-gate-statnive-live-asiatech-tehran.md). Schedule maps to doc 29 §8 W1–W5. Approved execution plan in `~/.claude/plans/phase-7e-load-gate-scaffolding-wise-puppy.md`. Two-track delivery: 🟢 **Fast probe — ✅ DONE (PR #94, 2026-05-04):** 300 EPS sustained on Netcup VPS 2000 G12, SLOs held 0→500 EPS, evidence at `releases/load-gate/fast-probe-${DATE}.md` + `test/perf/fast-probe.js`. 🔵 **Full gate** below — Locust + k6 + Vegeta/wrk2 + 7-chaos + observability VPS + generator_seq oracle on a Netcup VPS 2000 G12 SE staging clone (€11.49/mo); NOT Asiatech (Phase 10 P1 cutover runs against same harness).

- [ ] **W1** — Migration `007_load_gate_instrumentation.sql`: adds 4 instrumentation columns to `events_raw` (`test_run_id UUID`, `generator_node_id UInt16`, `test_generator_seq UInt64`, `send_ts DateTime64(3,'UTC')`) with typed DEFAULT sentinels (Rule 5 carve-out, doc 29 §6.1; >93.75% defaults preserve sparse serialization). Projection `proj_oracle` MATERIALIZEd. `{{if .Cluster}}` templated. `RawEvent` extended (omitempty); `clickhouse.go` batch column count 34→38. `dsar-completeness-checker` re-run.
- [ ] **W2** — Netcup VPS 2000 G12 SE staging clone (~30 min via `make airgap-bundle` + runbook SOP). Locust master + 3 FastHttpUser workers on separate Netcup VPS 1000 G12 SE (€5.99/mo, off-target per doc 29 §3.2). `test/perf/gate/{locustfile,locust-master}.py` — payload from `tracker/test/payload-golden.test.mjs`; bimodal session mix per doc 30 §4; 62/38 cohort split. Each event carries the 4-tuple. **12h capacity probe** (full 72h soak reserved for Phase 10): 0→300 EPS over 30 min, hold 11h, ramp; then 300→600 EPS breakpoint sweep in 10-min steps until SLO breach.
- [ ] **W3** — Observability VPS on third Netcup VPS 1000 G12 SE (€5.99/mo, separated per doc 29 §3.4). `deploy/observability/docker-compose.yml`: Prometheus + Grafana (gate-overview / oracle-queries / chaos-state / browser-cohort-slo) + Pyroscope (AGPL server OS-installed, Apache SDK linked) + Loki + Vector.dev (VRL PII scan halts on `rate(pii_hit)>0`) + Parca (eBPF, `--debuginfod-upstream-servers=""`) + Falco. `make observability-up` / `down`. `iptables -A OUTPUT -j DROP` burn-in confirms no outbound except scrape VLAN.
- [ ] **W4** — Seven chaos scripts (`A_bgp_cut.sh` … `F_clock_skew.sh` + `G_intl_egress.sh`) per doc 29 §5.1–§5.6 + doc 30 §3. Scenario G: 3h `tc netem` 80–120ms jitter + 2% loss on outbound Tehran-IX → Frankfurt peering, domestic NIN clean; both 62% Iran + 38% diaspora cohorts hit ≥99.5% attribution independently. Vegeta + wrk2 for HdrHistogram-precise breakpoints. Long-session memory-leak soak (`soak-memory-leak.py`, doc 30 §6) scaffolded for P4+P5. `chaos/dry-run.sh` — 2-node compressed-time cycle for CI smoke.
- [ ] **W5** — `make load-gate PHASE=P1 TARGET=staging`: oracle-scan → 12h/72h soak → 7-chaos → breakpoint → oracle-scan → emit `releases/load-gate/p1-staging-${DATE}.md` (verbatim oracle output, Grafana links, Pyroscope hash, replay attestation). Hard-fail on SLO breach. CI: `.github/workflows/load-gate-quarterly.yml` (workflow_dispatch); per-PR `load-gate-smoke` job (5-min × 300 EPS) in `ci.yml`. Phase 7e flips ⏳ → ✅; LEARN.md captures capacity numbers.
- [ ] **Makefile targets** — `make load-gate PHASE=Px`, `make capacity-probe`, `make chaos-matrix`, `make breakpoint TOOL=vegeta|wrk2`, `make oracle-scan RUN_ID=<uuid>`, `make soak-memory-leak` (P4+P5), `make observability-up` / `down`.
- [ ] **Synthesizer** (`test/perf/generator/main.go`) — emits the 4-tuple per event; supports SamplePlatform anonymized-NDJSON replay (Phase 10 P1) + synthetic fill (W2 probe). Kernel tuning per doc 29 §3.2 (`ulimit -n=1048576`, `tcp_tw_reuse=1`, `somaxconn=65535`, BBR) on every generator node.
- [ ] **Replay-attestation template** (`docs/replay-attestation-template.md`) — SamplePlatform analytics owner signs per-phase export (regex-scrub spec + salt rotation + auto-delete). Phase 10 P1 prep.
- [ ] **Acceptance** — P1 dry-run on Netcup SE 2-node staging passes all 7 chaos scenarios + 0→450 EPS breakpoint + zero loss/duplicates before Phase 10 Week 21.

**Cost envelope:** ~€23.47/mo across 3 Netcup SE boxes (1× target 8c/16GB + 2× generator/observability 4c/8GB), monthly billable, tear-down between windows. **Out of scope:** P2-P5 graduation on bigger Asiatech hardware (Phase 10 P1-P5); anonymized-NDJSON replay (Phase 10 P1 prep); v1.1 follow-ups.

### Phase 8: Deployment & Launch (Weeks 17–18) — Foundation for Milestone 1 (v1 binary ready)

**Guardrails:** [`air-gap-validator`](.claude/skills/air-gap-validator/README.md), [`clickhouse-rollup-correctness`](.claude/skills/clickhouse-rollup-correctness/README.md), [`clickhouse-cluster-migration`](.claude/skills/clickhouse-cluster-migration/README.md). Plus `AgriciDaniel/claude-cybersecurity` one-shot audit.

- [x] `make airgap-bundle` — binary + migrations + default YAML + tracker + SHA256SUMS + optional Ed25519 signature (PR #TBD). Docker tarball + bundled LITE DB23 BIN deferred to v1.1 (operator SCP + monthly refresh per runbook). `vendor/` tarball opt-in via `ENABLE_VENDOR_TAR=1` (audit tool, not runtime).
- [x] Deployment runbook (bare-metal, air-gapped bundle install) — `docs/runbook.md § Air-gap bundle install` + `§ GeoIP update procedure` + `§ Alerts file format` + `§ Log rotation` (PR #TBD).
- [x] File-sink alerts (`/var/log/statnive-live/alerts.jsonl`): WAL >80% / >90% / >95%, CH down, disk >85% / >90% / >95%, TLS expiry <30d / <7d (PR #TBD). `internal/alerts/` ships sink + band-tracker + prober loops; enter/exit bands paired so 6-polish-5's Notice UI can auto-dismiss on recovery. Syslog/Telegram = v1.1.
- [x] Offline GeoIP update procedure — `deploy/airgap-update-geoip.sh` (atomic-mv + SIGHUP + observe `geoip.reloaded` in `audit.jsonl`); internal NTP requirement documented at `docs/deployment.md:126`. SIGHUP-reload wired via `internal/enrich/geoip.go` atomic.Pointer swap + pre-swap probes (doc 28 §Gap 1).
- [ ] Netcup VPS 2000 G12 iv NUE hourly-based (Phase A, 8 vCore AMD EPYC x86_64 / 16 GB DDR5 ECC / 512 GB NVMe / 2.5 Gbit unlimited / Nuremberg, Germany / **€25.48/mo + €5 one-time setup**, no contract lock-in / IPv4 + IPv6 enabled — **procured 2026-04-24** as the doc 36 §4.1 fallback because Hetzner's photo-ID doc-verification blocks signup; Hetzner CX43 remains documented as the future Phase C upgrade once that account activates) OR Iranian DC (SamplePlatform) — Phase 9 dogfoods on real Netcup; Phase 10 P1 cuts over SamplePlatform. Phase 8 validated the install on `docker run ubuntu:24.04` throwaway + local CH only.
- [ ] Backup cron — SOP shipped in runbook § Backup & restore; cron line stays operator-owned (not enforced by install script).
- [ ] SamplePlatform tracker integration; v1 launch — Phase 10.

### Phase 8b: GHA release pipeline (~2 days) — collapses Phase 9/10 manual ops into one tag push

Twelve-factor V (build/release/run) seam over the existing `make airgap-bundle`. The bundle tarball stays the single distribution channel for both SaaS and self-hosted/Iranian DC; this phase only adds the **automation harness above** it. No binary changes.

- [x] [`statnive-live/.github/workflows/release.yml`](.github/workflows/release.yml) — shipped 2026-04-26. `push: tags: ['v*']` triggers `make release` (web-build + lint + test + test-integration + audit + airgap-bundle), Ed25519-signs via `STATNIVE_RELEASE_PRIVKEY` (GHA secret), `gh release create vX.Y.Z` attaches `*.tar.gz` + `SHA256SUMS` + `SHA256SUMS.sig`. `permissions: contents: write, actions: write`. SHA-pinned actions. Final `gh workflow run deploy-saas.yml` step closes LEARN.md Lesson 22.
- [x] [`statnive-live/.github/workflows/deploy-saas.yml`](.github/workflows/deploy-saas.yml) — shipped. `release: types: [published]` + `workflow_dispatch`. Downloads tarball, SCPs to Netcup, invokes on-box `/usr/local/bin/statnive-deploy` which verifies sig, atomic-symlinks `/opt/statnive-live/current`, `systemctl restart statnive-live`, polls `/healthz` ≤30 s. Required GHA secrets: `NETCUP_SSH_HOST` / `NETCUP_SSH_USER` / `NETCUP_SSH_KEY` / `NETCUP_SSH_KNOWN_HOSTS` / `STATNIVE_RELEASE_PRIVKEY`.
- [x] [`statnive-live/deploy/statnive-deploy.sh`](deploy/statnive-deploy.sh) — shipped. Restricted command provisioned at VPS setup time per [`docs/runbook.md § Provision a fresh Netcup VPS`](docs/runbook.md). LEARN.md Lesson 20 fix landed in `fix/v0.0.1-rc1-deploy-followups`: probe URL derived from `/etc/statnive-live/config.yaml` (`server.listen` + `tls.cert_file`).
- [x] [`statnive-live/.github/workflows/rollback-saas.yml`](.github/workflows/rollback-saas.yml) — shipped. `workflow_dispatch -f to=vX.Y.Z-1` reverts via the same restricted-command channel.
- [x] **Deliberate non-feature**: no Docker image, no container registry. Single-binary tarball is the only artifact for both SaaS and air-gap surfaces.
- [x] **Follow-up gaps from v0.0.1-rc1 cutover (`fix/v0.0.1-rc1-deploy-followups`).** Closes LEARN.md Lessons 20–23: (a) `statnive-deploy` healthz probe reads `server.listen` + scheme from config (Lesson 20); (b) `make ops-install-release-key VPS_HOST=...` target + runbook line for the one-time VPS pubkey prereq (Lesson 21); (c) final `gh workflow run deploy-saas.yml` step in `release.yml` so the chain auto-fires (Lesson 22); (d) `bin/statnive-live --check-embed-sizes` CLI gate + ERROR-level startup log on small crawler embed (Lesson 23). Verification §63–§65.

### Phase 9: Dogfood on statnive.com (Weeks 19–20) — 🎯 Milestone 1: statnive.com live

- [ ] **GDPR ops gate (HARD GATE on cutover — runbook-driven, ~30 min):** complete every step in [`docs/rules/netcup-vps-gdpr.md`](docs/rules/netcup-vps-gdpr.md) before the first EU visitor reaches the box. (1) Sign Netcup AV contract in CCP per §2 with the verbatim Annex 3 values; archive the returned PDF to `releases/infrastructure/netcup-dpa-2026-04-24.pdf` (gitignored, chmod 0600); record signing date + SHA-256 in [`docs/compliance/subprocessor-register.md`](docs/compliance/subprocessor-register.md). (2) Publish the §3 sub-processor list at `https://statnive.live/privacy`. (3) Add MX null + SPF + DMARC records to [`deploy/dns/statnive.live.zone`](deploy/dns/statnive.live.zone) per §4.2; enable DNSSEC at Cloudflare; verify with the dig commands in §8. (4) Apply server-side hardening above Netcup Annex 1 per §6 — **LUKS on `/var/lib/clickhouse` is required, not optional** (Netcup is shared-tenant virt; see [`docs/luks.md`](docs/luks.md) Tier matrix). (5) Pin chrony to Netcup's NTP peer per §6.6. (6) Tighten SSH per §6.4 + add `ops/ssh-allow-list.txt`. (7) Wire HSTS + OCSP stapling + monthly `testssl.sh` snapshot per §5.3. (8) Apply append-only `chattr +a` audit-log discipline + logrotate `copytruncate=off` per §6.5. (9) Set `vm.swappiness=1` per §6.11. SOPs land under [`docs/runbook.md` § Sign the Netcup DPA + populate the sub-processor register / § DNS hygiene / § Pin chrony / § SSH hardening / § TLS posture / § Audit-log append-only / § Swap policy](docs/runbook.md).
- [ ] **One-time provision** (operator, ~30 min, runbook-driven): Netcup VPS 2000 G12 iv NUE Nuremberg (8 vCore EPYC / 16 GB DDR5 / 512 GB NVMe / IPv4+IPv6 / **€25.48/mo + €5 setup**, procured 2026-04-24 per doc 36 §4.1) — install ClickHouse 24, install [`deploy/statnive-deploy.sh`](deploy/statnive-deploy.sh) at `/usr/local/bin/statnive-deploy`, drop `/etc/statnive/release-key.pub`, write systemd `Environment=` drop-in for `STATNIVE_MASTER_SECRET` + bootstrap admin + demo banner, configure restricted authorized_keys for the GHA deploy key. SOP: [`docs/runbook.md § Provision a fresh Netcup VPS`](docs/runbook.md). Phase C growth-tier path at ~10 paying customers: Hetzner AX41/AX42 (if doc-verification resolves) or Netcup RS 2000/4000 G12 — same `statnive-deploy.sh` runs unmodified.
- [ ] **One-time DNS + TLS** (operator, ~10 min): registrar A + AAAA for `statnive.live` + `app.statnive.live` + `demo.statnive.live` → Netcup; `certbot --standalone` issues one cert with 3 SANs; PEMs land in the loader paths; `cert.Loader` SIGHUPs without restart on rotation. Monthly renewal via cron + SIGHUP. SOP: [`docs/runbook.md § Issue + rotate TLS`](docs/runbook.md).
- [ ] **One-time GeoIP seed** (operator, ~2 min): IP2Location LITE DB23 manual SCP + `airgap-update-geoip.sh`; monthly refresh same way. Attribution surfaces (3) all shipped. Phase 10 P1 swaps to paid DB23 via the courier protocol.
- [ ] **One-time site seeding** (operator, ~3 min): Admin UI → Sites → add `statnive.com` (id=1), `statnive.de` (id=2), `fr.statnive.com` (id=3) + `demo/demo-statnive-2026` viewer scoped to site_id=1. SOP: [`docs/runbook.md § Seed 3 sites`](docs/runbook.md).
- [ ] **Subsequent deploys = one tag push.** `git tag vX.Y.Z && git push --tags` → `release.yml` builds + signs the bundle and creates the GitHub Release → `deploy-saas.yml` SSHes Netcup, atomic-symlinks the new release, reloads the binary, polls `/healthz` + `/api/about`. Failure auto-rollbacks. The 13-step manual ops checklist relocates to [`docs/runbook.md § Phase 9 breakglass — manual cutover`](docs/runbook.md) for use only when GHA is unavailable.
- [x] **Login page exposes demo creds** — `demoBanner` wire already present in `Login.tsx` (Phase 2b shipped it); set `STATNIVE_AUTH_DEMO_BANNER` via systemd drop-in at cutover.
- [x] **Tracker snippet in `statnive-website/` Astro base layout** — PR #TBD replaces `PostHog.astro` with `Tracker.astro` (`<script src="https://statnive.live/tracker.js" async defer>`) in `BaseLayout.astro`. Thirteen `window.posthog?.capture(...)` call sites across 8 files deleted outright (v1 tracker is pageview-only; custom events in v1.1). `/statnive-de-sync` + `/statnive-fr-sync` carry the delta into `statnive-de/` + `statnive-fr/` mirrors automatically.
- [ ] Acceptance (24h after cutover): non-zero visitors on all three `site_id`; viewer 403 on `/api/admin/*` (already tested at `rbac_test.go:23`); all 8 `/api/stats/*` return data; `alerts.jsonl` has no unresolved entries; **§8 GDPR verification block green** — `dig` for MX null + SPF + DMARC + DNSSEC RRSIG; `curl -sSI https://statnive.live | grep -i strict-transport-security`; `lsblk -f | grep crypto_LUKS` on the CH data volume; `chronyc sources` points at the Netcup peer; `curl -sS https://statnive.live/privacy | grep -q ANEXIA`; signed Netcup DPA PDF present at `releases/infrastructure/netcup-dpa-2026-04-24.pdf` and recorded in [`docs/compliance/subprocessor-register.md`](docs/compliance/subprocessor-register.md). Full check: [`docs/runbook.md § Milestone 1 acceptance check`](docs/runbook.md) + [`docs/rules/netcup-vps-gdpr.md § 8`](docs/rules/netcup-vps-gdpr.md#8-verification-how-to-know-this-is-done).

### Milestone 1 cutover postmortem (2026-04-25)

Moved to [`PLAN-MILESTONE-1.md`](PLAN-MILESTONE-1.md). Frozen historical record from the v0.0.1-rc1 → rc8 cutover; 24-item bug-class catalog organised CRITICAL / HIGH / MEDIUM / LOW. Consult before planning any future release procedure.

### Phase 10: SamplePlatform dedicated Iranian VPS (Weeks 21–24) — 🎯 Milestone 2: SamplePlatform live (P1 cutover; P2–P5 roll out over ~10 months post-M2)

**HARD GATE on cutover:** [`ratelimit-tuning-review`](.claude/skills/ratelimit-tuning-review/README.md) — Iranian-ASN compound-key tiering before the first byte. AS44244 / AS197207 / AS57218 on `(ip, site_id)` at 1K req/s sustained / 2K burst; 100/200 fallback elsewhere; 25K req/s per-site global cap. ASN via `iptoasn.com` public-domain TSV (MaxMind / IPLocate CC-BY-SA rejected). k6 scenarios `normal`/`burst`/`ddos`/`cgnat` must pass.

**Onboarding app-by-app.** Full StreamCo-class (MAX: 5M DAU / 200M events/day) requires a cluster. Enter with **web only** (MIN: ~200K DAU / 3M views/day — 30× smaller), onboard iOS/Android/TV across months 1–12.

**DNS & shutdown-resilience (non-negotiable, dual-domain Architecture C — disjoint-customer-set carve-out from [doc 26 § 3.3 + § 7.2](../jaan-to/docs/research/26-iran-shutdown-dns-strategy.md)):** Two independent zones serving disjoint customer sets. No GeoDNS, no AXFR, no shared zone, no client-side region detection.
- **`statnive.live`** (international SaaS only — Phase A dogfood + Phase C signups) — authoritative DNS at **Cloudflare free tier** (chosen 2026-04-25; permitted on this zone only because no Iranian resolver ever queries it). 12 records imported in DNS-only mode (grey cloud) per the canonical zone file [`deploy/dns/statnive.live.zone`](deploy/dns/statnive.live.zone). Records resolve to Netcup VPS 2000 G12 NUE — IPv4 `94.16.108.78`, IPv6 `2a03:4000:51:f0c::1` (chosen from the `2a03:4000:51:f0c::/64` subnet; bound on the VM via netplan per `docs/runbook.md` § Bind IPv6 on Netcup VM).
- **`statnive.ir`** (SamplePlatform + future Iranian customers — Phase B only) — authoritative DNS via self-hosted NSD on Asiatech AT-VPS-B1 (~7M Rial/mo). Single primary, no secondary. Iranian resolvers reach NSD over NIN; reachability survives 2019 / 2022 / 2025 / 2026 shutdown shapes because both DNS and origin sit inside Iran on Tehran-IX. **No Cloudflare on this zone** per [`iranian-dc-deploy`](.claude/skills/iranian-dc-deploy/README.md) `iran-no-cloudflare` rule. Registration via Pars.ir in EUR/USDT (~$15/yr). Optional `statnive.ایران` IDN reserved at IRNIC (v1.1).
- **No cross-zone redirect.** SamplePlatform staff use `statnive.ir` exclusively; international customers use `statnive.live` exclusively. Tracker URL is hardcoded per-deployment at install time, never picked client-side.
- **Cost:** ~$15/yr (.ir registration) + ~7M Rial/mo NSD VPS (folded into Iranian-DC ops budget) + $0–$1/mo for `.live` DNS.
- **Doc 26 reconciliation done 2026-04-25:** doc 26 carries supersession callouts at the top + § 3.3a carve-out + § 7.1 / § 7.4 elevation of Architecture C as the chosen recommendation. doc 26 Architecture B text remains valid for the unified-customer case.

Per-phase Iranian DC sizing:

| Sub-phase | Scope | Max DAU | Max MAU | Max daily events | Max monthly events | Asiatech server | Price/mo |
|---|---|---|---|---|---|---|---|
| **P1** [MIN] | Web views | 200K | 1.4M | 3M | 75M | `AT-VPS-G2` | 27.9M Rial |
| **P2** (+1mo) | +curated interactions | 200K | 1.4M | 15M | 350M | `AT-VPS-G2` | 27.9M Rial |
| **P3** (+3mo) | +iOS | ~1.45M | ~5.65M | 70M | 1.4B | `AT-VPS-A1` + BW ≥500 GB/mo | 63.5M + BW |
| **P4** (+6mo) | +Android | ~3.45M | ~12.45M | 140M | 3B | Dedicated 16–32c/64–128GB/2TB NVMe + ≥1 TB/mo | quote |
| **P5** (+10mo) [MAX] | +TV + HA | 5M | 17M | 200M | 4B | Cluster 2–3× (32c/128GB/4TB NVMe) + unmetered | quote |

- [ ] P1 cutover: Asiatech G1/G2 standard VPS (~15–28M Rial/mo, 150 GB/mo fits web)
- [ ] Negotiate P3+ quotes: Asiatech BW upgrades + dedicated from Asiatech/Shatel/Afranet/ParsPack
- [ ] D2 provisioning (VPS P1/P2 → dedicated P3)
- [ ] DNS: `A/AAAA SamplePlatform.statnive.ir → <Iranian-DC-IP>` in NSD zone file on AT-VPS-B1. **No Cloudflare** (`iran-no-cloudflare`). NS delegation at IRNIC: `ns1.statnive.ir` glue → AT-VPS-B1 IP.
- [ ] **Courier-driven install** — `make release-iran-vps` from an outside-Iran trusted bastion (recommended: a self-hosted GHA runner outside Iran, triggered via `gh workflow run release-iran.yml -f version=vX.Y.Z -f host=… -f license=…`; fallback: operator laptop). Pulls the bit-identical release tarball from GitHub Releases (Phase 8b's signed artifact), stages a courier overlay (license JWT age-decrypted on demand, paid IP2Location DB23 BIN, cert-forge-issued PEMs for the `statnive.ir` SAN list per CLAUDE.md "no LE in Iran" anti-pattern — `.ir` cert chain is independent from the international `.live` chain under Architecture C), `rsync` over a single SSH connection into the Iranian VPS, runs the existing `deploy/airgap-install.sh --bundle … --license … --geoip … --tls … --apply-iptables`, then `make smoke` (Phase 5a-smoke) + `npm --prefix web run e2e` (Phase 5c) against the freshly-installed binary. **The Iranian VPS itself has zero outbound at any point** — install, runtime, or update — preserving the air-gap Isolation invariant. Wrapper script: [`deploy/courier-iran.sh`](deploy/courier-iran.sh). Outside-Iran bastion is itself a sub-processor under GDPR — register before first courier run per [`docs/rules/netcup-vps-gdpr.md § 3`](docs/rules/netcup-vps-gdpr.md).
- [ ] Manual PEM (LE throwaway or SamplePlatform internal CA), quarterly rotation
- [ ] **Paid IP2Location DB23** on D2 only (city accuracy matters)
- [ ] Ed25519 license JWT (`site_id=1, Customer="SamplePlatform", MaxEventsDay=0, Features=["*"], ExpiresAt=+1y`). Private key age-encrypted on offline laptop.
- [ ] Config overrides: `audit.sink=file`, `license.phone_home=false`. Only `site_id=1`.
- [ ] Seed `sites` with SamplePlatform hostnames (`SamplePlatform.com`, `www.SamplePlatform.com`, CDN subdomains)
- [ ] Admin user → secure channel (Signal / in-person / PGP)
- [ ] SamplePlatform pastes `<script src="https://SamplePlatform.statnive.ir/tracker.js" defer></script>` + root-domain cookie walking (Clarity pattern, doc 21)
- [ ] Acceptance per sub-phase (doc 29 §4): P1 72h soak @ 240 EPS + 7-scenario chaos (+ G international-egress per doc 30 §3) + 0→450 EPS breakpoint → binary SLO sign-off before SamplePlatform web cutover; P2/P3/P4/P5 repeat the gate at their respective envelopes (1K / 4K / 9K-18K match / 40K peak) before onboarding each app. PII wire-scan `rate()` = 0 throughout. Air-gap end-to-end + backup+restore remain prerequisite (§17 / §37).

### v1.1 — Pre-SaaS prerequisites

These three v1.1 items block Phase 11a or 11b. Each can fold into its blocking milestone's PR (ship the endpoint alongside the feature that needs it) or land standalone — decision made at execution time based on PR-review bandwidth.

- **v1.1-tokens — API-token rotation endpoint** (blocks 11a). `POST /api/admin/tokens` hashes + stores a new raw token; `DELETE /api/admin/tokens/{label}` revokes. Today tokens live in config or env (`STATNIVE_API_TOKENS`) and require a binary restart to rotate — unacceptable for multi-tenant SaaS where a leaked token from one customer's CI must be revokable without downtime for others. Reuses Phase 2b's `APIToken` hashed-by-SHA-256 pattern; adds a small CH table + admin handler. **Includes UI surface** ([doc 35 §5 R12](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md)): new Admin sub-tab next to Sites / Users / Goals — list (label, last-rotated, scope), inline create form, revoke button, download-once-on-create modal (raw token shown exactly once, then only the SHA256 is stored). ~0.5 day frontend on top of the endpoint.
- **v1.1-pwreset — Password-reset email flow** (blocks 11a). Forgot-password link on `/app/login` → `POST /api/auth/password-reset-request` (email + rate-limit) → signed time-limited token via email → reset landing page → `POST /api/auth/password-reset`. Requires `email.enabled` SMTP config. NIST 800-63B password policy (8+ chars, HaveIBeenPwned top-10k blocklist) ships in this slice alongside the signup path in Phase 11a.
- **v1.1-csrf — CSRF double-submit + step-up auth** (blocks 11b). Double-submit token cookie + request header on every `/api/admin/*` mutation; `RequireFreshAuth(5m)` on privilege-granting handlers (password change, role change, billing change, token rotation). SameSite=Lax held the line for Phase 2b; the paid-tier multi-tenant surface raises the value of a leaked admin cookie, so belt-and-braces applies here.
- **v1.1-onboarding-email — Branching transactional flow** (doc 32 §C / doc 33 §6.3; optional prereq to 11a). Three emails with branching on activation state: **T+1 h** if `events.count(site_id)=0` → install-help, link to `docs/quickstart.md`; **T+24 h** if activated but no goal → "create your first goal" walkthrough; **T+7 d** if active → real-person reply invitation (reply-to = operator, measured by reply rate). Pause sequence if 2 consecutive emails fail to flip activation (PostHog's "v5 → simpler" lesson, doc 32 §C.6.3). SMTP plumbing reuses v1.1-pwreset's `email.enabled`. Self-hosted deployments skip by leaving `email.enabled=false`. ~3 days on top of v1.1-pwreset.
- **v1.1-install-cli — Deterministic tracker-install CLI** (doc 31 §"concrete recommendation" / doc 34 §A "minimal equivalent worth building"; optional prereq to 11a for non-WP frameworks). `statnive-install` sub-command of the binary: framework detector (WordPress `wp-config.php`, Next.js `package.json` + `next` dep + `app/` vs `pages/` router shape, Astro `astro.config.*` + adapter scan, plain HTML fallback) → diff preview → operator confirm → snippet insertion + `.bak` backup → synthetic event round-trip verification. **Zero LLM, zero outbound, zero repo-content-leaves-laptop**. Explicitly REJECTS PostHog's cloud-agent `npx @posthog/wizard` model per doc 34 §"Reject as default" — the failure modes (corrupted `.env.local`, Astro frontmatter mangling, OAuth break) are not worth the ergonomic win. WP + Next.js + plain HTML for v1.1; Laravel + Django + Flask + Astro = v1.2 fast-follow. ~1 week.
- **v1.1-a11y — WCAG 2.2 AA gate + chart a11y** ([doc 07](../jaan-to/docs/research/07-gaps-wordpress-org-submission-pre‑submission-audit.md) + [doc 35 §5 R13 + §2.8](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~1 week + ~0.5 day for (d)). Four deliverables: (a) `@axe-core/playwright` integrated into the existing Playwright suite at `web/e2e/` and asserted as zero-violation on every PR touching `web/`; (b) hidden `<table>` mirror or `aria-describedby` summary on every uPlot chart so screen-reader users get the data — uPlot canvas is not natively a11y-readable; (c) keyboard-trap audit pass against the four-row sticky `AppShell.tsx` (sticky-header focus-obscuration is the obvious risk surface); (d) extend `prefers-reduced-motion` honoring beyond the existing `LivePulse` to **all** transitions — LiveDot, sparkline animation (when 6-polish KPI sparklines land), modal open/close, `Phase 6-polish-5` toast slide-in, DeltaPill ↑/↓ flash (WCAG 2.2 §2.3.3). Phase 5e contrast is already AAA (~27:1 navy on paper) — this slice closes the gaps that contrast can't. Bundle gate impact: zero (CI tooling + ARIA-only primitives + media-query guards). **Optional prereq to 11a** — once the dashboard is internet-facing, an undocumented a11y posture is enterprise/legal liability; recommend folding into 11a's PR.
- **v1.1-sops-secrets — SOPS+age encrypted secrets in repo** (optional posture upgrade; ~2 days). Twelve-factor III "config in env vars" extended via decrypt-on-boot drop-in. `deploy/secrets.env.age` lives in git, encrypted with the operator's age public key; `deploy/airgap-install.sh` decrypts it at install time using the age private key supplied via `--age-key` (held outside the repo on the operator laptop or GHA secret store). Single git tag then carries binary + config + secrets atomically — every rotation is a commit, fully audit-traceable. Default v1 posture (GHA secrets injected into the systemd drop-in) stays the simpler path; SOPS is for customers who want pure git-tracked deploy artifacts. Self-hosted-only by default; SaaS keeps GHA-secrets path until/unless the audit value justifies the key-management overhead. Skip if no customer asks; not on Milestone 1/2 critical path.
- **v1.1-frustration-panel — Operator surface for the Safe Autocapture Pack** ([doc 21](../jaan-to/docs/research/21-microsoft-clarity-codebase-review-patterns-for-self-hosted-analytics.md) + [doc 32 §A](../jaan-to/docs/research/32-posthog-statnive-deep-research.md) + [doc 35 §5 R7](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~3–5 days frontend on top of the Pack itself). New lazy "Health" panel rendering rage clicks (>3 clicks/sec on the same element — match PostHog threshold for cross-tool comparability), dead clicks, p75 INP, p75 LCP, Long Task counts (>50 ms main-thread blocks via `PerformanceObserver`), per page path, sortable, with sparkline trends. Pairs with the v1.1 Safe Autocapture Pack (`pack-safe.js`, CLAUDE.md §"Deliberate skips"): the pack emits client-side, this slice renders server-side after rollup. Backend: extend `events_raw` enrichment to recognize `pack-safe.*` event types + new rollup `daily_frustration` (AggregatingMergeTree, `ORDER BY (site_id, day, path)` per Architecture Rule 8). Frontend: new lazy panel `web/src/panels/Health.tsx`, ~6–8 KB gz (under the 10 KB lazy-panel gate). **The deliberate substitute for session replay** — operators get diagnostic value without recording user content. Optional prereq to Phase 10 P1 cutover so SamplePlatform operators get the diagnostic surface alongside the Pack.

### Phase 11a-pre: GDPR ops gate (HARD GATE on first paying SaaS customer)

> Not on the **Milestone 1 functional** gate (that closes when the 24h soak passes — the binary works, the bug catalog is closed, the postmortem is documented). This is the **commercial** gate that opens the SaaS for first paying customer. Independent track from 11a code work; can run in parallel.

| # | Item | Owner | Source-of-truth |
|---|---|---|---|
| 1 | **Sign Netcup Art. 28(3) DPA** in CCP (Customer Control Panel → "Vertragsdatenschutzvereinbarung"). Annex 1 TOM acknowledged in writing. | ops | [`docs/rules/netcup-vps-gdpr.md` § 1](docs/rules/netcup-vps-gdpr.md) |
| 2 | **Populate sub-processor register** with Netcup as the data-host sub-processor. Customer-facing list at canonical URL; first paying customer DPA references this URL. | ops | [`docs/compliance/subprocessor-register.md`](docs/compliance/subprocessor-register.md) |
| 3 | **Publish `/privacy` page** on `statnive.com`, `.de`, `.fr` mirrors (sync via `/statnive-de-sync` + `/statnive-fr-sync`). Cookie row reflects Option C: `_statnive` is set only after explicit consent OR in non-EU self-hosted deployments. | ops + counsel | `statnive-website/src/content/legal/privacy.mdx` |
| 4 | **DNS hygiene** — MX null record (`MX 0 .` to assert "no email"; prevents spoofed mail with `From: *@statnive.live`); SPF `v=spf1 -all` on `statnive.live`; DMARC `p=reject; rua=mailto:dmarc@statnive.live`; DNSSEC enabled at the registrar (Cloudflare zone holds the DS records). | ops | `deploy/dns/statnive.live.zone` |
| 5 | **LUKS on `/var/lib/clickhouse`** — re-image VPS once with full-disk encryption OR pivot only the CH data volume. Document the unlock path (operator-typed passphrase at boot via `dropbear-initramfs` SSH or KMS-style remote unlock; pick one + document the recovery procedure). 40–50% I/O overhead acknowledged + benchmarked. | ops | [`docs/luks.md`](docs/luks.md) |
| 6 | **NTP pinned to Netcup** (`ntp.netcup.de` via chrony) — IRST salt rotation depends on correct date; CLAUDE.md Privacy Rule 2 invariant. Currently using system NTP defaults; migrate to chrony with the Netcup pool + monitor `chronyc tracking` reachability. | ops | [`docs/rules/security-detail.md`](docs/rules/security-detail.md) § NTP |
| 7 | **HSTS preload + monthly testssl** — submit `app.statnive.live` to the Chromium HSTS preload list (`hstspreload.org`) once `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` is live for 30 days. Monthly `testssl.sh` cron emits to `/var/log/statnive-live/testssl-YYYY-MM.txt`; alert on any A- or below. | ops | [`docs/rules/security-detail.md`](docs/rules/security-detail.md) § TLS |
| 8 | **`vm.swappiness=1`** — minimize swap use (avoids ClickHouse cold pages getting evicted to disk under memory pressure; mirrors the upstream CH-recommended sysctl). Persist via `/etc/sysctl.d/60-statnive.conf`. | ops | [`docs/rules/netcup-vps-gdpr.md` § 6](docs/rules/netcup-vps-gdpr.md) |
| 9 | **Counsel review checkpoint** — independent legal counsel reviews: (a) `docs/dpa-draft.md` final draft, (b) the publicly-rendered `/privacy` page wording, (c) sub-processor register language, (d) the cookie posture decision in [`docs/privacy/cookie-posture.md`](docs/privacy/cookie-posture.md) (Option C shipped — confirms it satisfies "strictly necessary" criteria OR flags a gap). Sign-off recorded in `docs/compliance/counsel-review.md` with reviewer + date. **Blocks first paying SaaS signup unconditionally.** | ops + counsel | NEW `docs/compliance/counsel-review.md` |

**Acceptance:** all 9 items have a green check. Items 1–8 are operator action; item 9 is the legal sign-off. Without all 9, Phase 11a may ship the code but the SaaS doesn't open for paid signup.

### Phase 11a: SaaS self-serve signup — free tier (🎯 Milestone 3, Weeks 26–28)

**HARD GATE on first signup:** [`gdpr-code-review`](.claude/skills/gdpr-code-review/README.md) + [`dsar-completeness-checker`](.claude/skills/dsar-completeness-checker/README.md) paired. 12-item privacy-by-design + sink-matrix integration test (`system.tables` enumerated dynamically). DPA draft at `docs/dpa-draft.md` with doc 27 §line 77-79 HLL-anonymous language. Weekly rollup rebuild cron (`robfig/cron`, Sunday 03:00 IRST) as bounded-time safety net. Applies to 11a because this is when the first public tenant lands — even free-tier tenants are covered by GDPR.

- [ ] `POST /api/signup` (email + password + hostname → site + admin user). Reuses `httpjson.DecodeAllowed` from Phase 3c for the F4 mass-assignment guard (site_id + role sourced from server-side, never body).
- [ ] **Path-based tenant routing** in `dashboard/router.go` — `app.statnive.live/s/<slug>/...`; middleware extracts slug → `site_id` via `internal/sites/sites.go`.
- [ ] Slug generation / uniqueness / blocklist — **already shipped in Phase 3c** (`sites.GenerateSlug`, `sites.IsSlugAvailable`, `sites.ReserveSlug`, `sites.ReservedSlugs`). Phase 11a just calls them from the signup handler.
- [ ] Signup guardrails: DNS-resolvable hostname, not on blocklist, unique in `sites`, 5/hr/IP rate-limit.
- [ ] Email verification with 24-hour grace window — signup creates the tenant in a `pending` plan; first event emits a tracker warning banner until email is verified; 24 h after signup, unverified tenants get disabled.
- [ ] Free tier 10K PV/mo via `daily_users` (v1.1) or `count(DISTINCT visitor_hash)` over `hourly_visitors`; soft throttle + `quota_exceeded=1` tag + upsell banner.
- [ ] Onboarding at `/s/<slug>/onboarding` (copy-paste tracker snippet + "Check for first event" button triggering a user-initiated refresh).
- [ ] Email transactional (signup confirmation + welcome) — opt-in via `email.enabled`. Password-reset email path comes from v1.1-pwreset (landing here or shipping standalone).
- [ ] **Mass-assignment guard review on every new write endpoint (F4, Verification §52).** `POST /api/signup` + any new admin endpoint in 11a uses the `httpjson.DecodeAllowed` helper established in Phase 3c; sensitive fields (`site_id`, `role`, `plan`) never come from the request body. Laravel-style mass-assignment (`site_id=2, role=admin`) is how cross-tenant privilege escalation sneaks in — re-audit every new handler before merge.
- [ ] Acceptance: fresh signup → tracker snippet pasted → first event visible in dashboard <5 min; cross-site isolation (URL manipulation blocked); 6th signup/hr from same IP rejected; unverified tenant disabled at T+24h.
- [ ] **Activation funnel on admin dashboard** (doc 32 §C / doc 33 §6 — PostHog's per-product activation model). **Intent**: `admin.signup.complete` + `admin.snippet.copy` audit events. **Activation**: first event ingested + one `/api/stats/overview` view + one goal created (reuses Phase 3c goals CRUD). **Retention signal**: admin login on 3 distinct days within a 14-day window. Ships as read-only `/app/#admin/activation` panel; data lives in tenant's own `events_raw` + `audit.jsonl`; zero schema impact beyond 2 new audit-event constants.

### Phase 11b: SaaS paid tiers — Polar.sh (🎯 Milestone 4, Weeks 28–30)

- [ ] `POST /api/billing/checkout` → `POST api.polar.sh/v1/checkouts/` with `external_customer_id=site_id`. No Go SDK — REST calls directly. Sandbox: `sandbox-api.polar.sh`.
- [ ] `POST /api/admin/billing` (Polar.sh webhook — verify `X-Polar-Signature` HMAC-SHA256; `subscription.{created,updated,canceled}` only; idempotent by `event.id`). Source of truth for `sites.plan`.
- [ ] Polar.sh (Merchant of Record) → no per-country tax registration. 4 Products × monthly+yearly (Free self-hosted only, Starter $9, Growth $19, Business $69, Scale $199). Customer Portal + Benefits = v2; v1 = email support.
- [ ] Paid tiers unlock quota (Starter 100K PV, Growth 1M, Business 10M, Scale 100M) + Funnel CRUD (Goals CRUD already free per Phase 3c — recommend leaving them free). Gate keyed by `sites.plan`.
- [ ] Email transactional (receipt/quota) — opt-in via `email.enabled`.
- [ ] Acceptance: sandbox `subscription.created` → `sites.plan` flips; `subscription.canceled` reverts at period end; webhook is idempotent against duplicate Polar deliveries; upgrade-banner click → Polar checkout → return → plan flip within 30s.

### Phase flags: Statnive Flags — local-first feature flags (🎯 Milestone 5, Weeks 31–33)

**Guardrails:** [`blake3-hmac-identity-review`](.claude/skills/blake3-hmac-identity-review/README.md), [`tenancy-choke-point-enforcer`](.claude/skills/tenancy-choke-point-enforcer/README.md), [`air-gap-validator`](.claude/skills/air-gap-validator/README.md).

Research anchor: doc 32 §A / doc 34 §C — the closest public pattern to Statnive's air-gap requirement. PostHog's local evaluation drops flag latency from ~500 ms → 10–20 ms and works fully offline; doc 34 calls it "almost a blueprint" for a Statnive product.

- [ ] Signed flag definitions (Ed25519, same key pattern as license JWT) SIGHUP-reloaded from `config/flags.json` — never poll from remote; definitions are an operator-deployed artifact, not a runtime fetch.
- [ ] In-memory Go eval; stable bucketing via BLAKE3 on `(flag_key, visitor_hash)`. Deterministic across restarts.
- [ ] Ban dynamic cohorts / remote targeting / persisted flag values — anything that breaks offline eval is anti-feature by design (doc 34 §C explicit warning adopted as a rule).
- [ ] Schema: migration `006_flags.sql` (ReplacingMergeTree, `{{if .Cluster}}` templated); `FlagsSnapshot` atomic.Pointer hot-swap mirroring `goals.Snapshot` (Phase 3c pattern).
- [ ] Handler `/api/admin/flags` CRUD reusing `httpjson.DecodeAllowed` (Phase 3c mass-assignment guard, Verification §52).
- [ ] Tracker module: `statnive.flag(key) → boolean | string` resolves against a locally-fetched, signed snapshot served by the binary; evaluates in-browser with the same BLAKE3 bucketing.
- [ ] `$flag_called` emitted as bundle-internal plumbing event — never billable per doc 33 §2 pricing principle ("dogfooded events across products are free").
- [ ] **Flag debug toolbar** ([doc 32 §A](../jaan-to/docs/research/32-posthog-statnive-deep-research.md) + [doc 34 §C](../jaan-to/docs/research/34-posthog-mechanics-and-portability-to-statnive.md) + [doc 35 §2.10](../jaan-to/docs/research/35-ui-ux-dashboard-evidence-synthesis.md); ~0.5 day on top of the slice). Admin-only panel surface (or behind a `?flags=debug` query-string toggle, mirroring the existing `?debug=1` tracker-verify pattern from 6-polish-2) showing for each flag: which value evaluated, the rule that fired (constant / cohort / percentage), the bucketing inputs (`flag_key` + `visitor_hash`), and the snapshot SHA256 the eval used. Bundle: ~1 KB gz lazy chunk (only loads when the toggle is on). Critical for diagnosing "why didn't this flag fire for this visitor" without remote evaluation logs.
- [ ] **Acceptance:** flag eval p99 <5 ms in-process; 100% offline under `iptables -P OUTPUT DROP` (blackout-sim, Verification §37); flag change → new eval result within 1 s of SIGHUP; cross-tenant isolation enforced by `whereTimeAndTenant` + `rollup-join-tenancy` (Verification §50).

## P0 Audit Backlog — `detect-live` (2026-05-04)

Sourced from the `/jaan-to:detect-{dev,design,writing,product,ux}` audit pack at [`../jaan-to/outputs/detect-live/`](../jaan-to/outputs/detect-live/) against `statnive-live` @ `89ab36d`. Selected P0s only — full severity matrix in the per-domain audit files. Items here all land in **v1.1** unless flagged otherwise; the multi-tenancy + value-prop items are Phase 11b prerequisites.

> **Cross-cutting theme — error-string handling (resolves 7 P0s with one workstream).** Build a shared `<ErrorState>` Preact component + a Go-error → user-string sentinel-translation layer between handlers and the dashboard. Closes **E-WRT-WEB-002**, **E-WRT-WEB-003**, **E-WRT-BACKEND-001**, **E-WRT-BACKEND-002**, **E-WRT-GLOSS-001**, **G-002**, **PP-002** (and improves **PP-001**) in one slice. Sentinel layer maps `errors.Is(err, store.ErrWALBackPressure)` → user-facing `"We're catching up on a traffic spike — your event was queued."` with no leakage of internal terms (`WAL`, `events_raw`, package prefixes). `<ErrorState>` provides retry button + recovery hint per error class. Recommend ~3 days; lands ahead of any SaaS public-self-serve copy.

### dev (1 P0)

- [ ] **E-DEV-028** [High, cicd] — Release pipeline does not generate or attach an SBOM. Add CycloneDX SBOM via `cyclonedx-gomod` + sigstore attestation to `release.yml`. ~2-min CI delta. → [`detect-live/dev/cicd.md`](../jaan-to/outputs/detect-live/dev/cicd.md). **Fits Phase 8b** alongside the existing GHA release pipeline work.

### writing (7 P0)

- [ ] **E-WRT-WEB-002** [High] — Raw backend Go error sentinels leak to dashboard UI. → [`detect-live/writing/writing-system-web.md`](../jaan-to/outputs/detect-live/writing/writing-system-web.md). *Closed by cross-cutting workstream above.*
- [ ] **E-WRT-WEB-003** [High] — User-facing errors lack recovery guidance. → [`detect-live/writing/writing-system-web.md`](../jaan-to/outputs/detect-live/writing/writing-system-web.md). *Closed by cross-cutting workstream above.*
- [ ] **E-WRT-BACKEND-001** [High] — User-facing HTTP errors are too terse for client surfaces. → [`detect-live/writing/writing-system-backend.md`](../jaan-to/outputs/detect-live/writing/writing-system-backend.md). *Closed by cross-cutting workstream above.*
- [ ] **E-WRT-BACKEND-002** [High] — Error sentinels carry package prefixes that leak to clients. → [`detect-live/writing/writing-system-backend.md`](../jaan-to/outputs/detect-live/writing/writing-system-backend.md). *Closed by cross-cutting workstream above.*
- [ ] **E-WRT-GLOSS-001** [High] — Internal term `WAL` leaks to end users via 503 error. → [`detect-live/writing/glossary.md`](../jaan-to/outputs/detect-live/writing/glossary.md). *Closed by cross-cutting workstream above.* Sentinel-translation layer must remap `wal back-pressure` 503 to user-language.
- [ ] **E-WRT-UICOPY-001** [High] — 4 of 8 NNg UI categories absent or near-empty (no toasts, no success states, no confirmation dialogs, no first-run onboarding copy). → [`detect-live/writing/ui-copy-web.md`](../jaan-to/outputs/detect-live/writing/ui-copy-web.md). Pairs with G-002 / G-003 frontend work.
- [ ] **E-WRT-UICOPY-002** [High] — Form labels have no helper text or validation feedback. → [`detect-live/writing/ui-copy-web.md`](../jaan-to/outputs/detect-live/writing/ui-copy-web.md). Affects every Admin form (sites, users, goals).

### product (3 P0)

- [ ] **E-PRD-ENT-006** [High, **Phase 11b prerequisite**] — Site ownership check is missing. `/api/stats/?site=N` doesn't verify `ctx.User` owns site N; any authenticated user can read any site by changing `?site=`. Single-tenant safe today; **breaks the moment Phase 11b SaaS lands** with multiple paying tenants. Fix: add `user_sites (user_id, site_id, role)` migration + `RequireSiteAccess(ctx, siteID)` middleware between `RequireRole` and the handler. Goals snapshot pattern (`atomic.Pointer` hot-swap) is the right shape for site-membership caching. → [`detect-live/product/entitlements.md`](../jaan-to/outputs/detect-live/product/entitlements.md). **Hard gate before Phase 11b cutover.**
- [ ] **E-PRD-PROP-001** [High, marketing] — Sole tagline ("Privacy-aware analytics for high-traffic websites") is generic to the category — Plausible, Fathom, Aptabase, Simple Analytics all claim it verbatim. → [`detect-live/product/value-prop.md`](../jaan-to/outputs/detect-live/product/value-prop.md). Lives in [`statnive-website/`](../statnive-website/) homepage hero copy, not this binary.
- [ ] **E-PRD-PROP-002** [High, marketing] — Air-gap differentiator (zero-outbound under `iptables OUTPUT DROP`) is unique in the analytics market but absent from any public copy. → [`detect-live/product/value-prop.md`](../jaan-to/outputs/detect-live/product/value-prop.md). Lives in `statnive-website/`. Pair with E-PRD-PROP-001 in a single homepage rewrite.

### ux (3 P0)

- [ ] **A11Y-004** [High, accessibility] — DualBar visualization has no text alternative. Screen-reader users get no signal on the primary attribution table. Add `<table>` shadow DOM or visually-hidden text alternative per visualization. → [`detect-live/ux/accessibility.md`](../jaan-to/outputs/detect-live/ux/accessibility.md).
- [ ] **G-002** [High] — Shared `<ErrorState>` component absent; 9 bespoke error blocks duplicate `"could not load — see logs"`. → [`detect-live/ux/gaps.md`](../jaan-to/outputs/detect-live/ux/gaps.md). *Closed by cross-cutting workstream above.* This is the Preact-side half of the cross-cutting fix; the Go sentinel-translation layer is the backend half.
- [ ] **PP-002** [High] — Overview "refresh page to reload" instruction is a usability cop-out. → [`detect-live/ux/pain-points.md`](../jaan-to/outputs/detect-live/ux/pain-points.md). Replace with explicit refresh button using existing TanStack Query / signals refetch — no new state machinery needed.

### Roadmap placement

| Phase / surface | Items | Notes |
|---|---|---|
| **Phase 8b** (release pipeline) | E-DEV-028 | Add SBOM + sigstore step alongside the existing `release.yml`. |
| **Phase 11b prerequisite** (HARD GATE before paid SaaS) | E-PRD-ENT-006 | Multi-tenancy site-ownership check; non-negotiable before non-trusted operators share a binary. |
| **v1.1 — error-handling workstream** (cross-cutting, ~3 days) | E-WRT-WEB-002 / WEB-003 / BACKEND-001 / BACKEND-002 / GLOSS-001, G-002, PP-002 | One PR shaped as Go sentinel-translation layer + Preact `<ErrorState>` component + retry-button affordance + sentinel→user-string registry. |
| **v1.1 — UI copy + a11y polish** | E-WRT-UICOPY-001, E-WRT-UICOPY-002, A11Y-004 | Pairs with the standard UI-polish phase; no architectural changes. |
| **`statnive-website/` (separate submodule)** | E-PRD-PROP-001, E-PRD-PROP-002 | Homepage rewrite — air-gap claim + non-generic tagline. Not in this binary. |

> **Items deliberately NOT promoted to PLAN.md.** The detect-live audit also surfaced E-WRT-WEB-001 (i18n absent on a Persian-market product, **Critical**), E-PRD-MON-001 (monetization 0% built — already tracked as Phase 11b), E-PRD-INS-006 (zero self-instrumentation), E-DSN-028 / A11Y-001 (skip-link missing), E-DSN-035 (no CODEOWNERS), E-DSN-019 (single mobile breakpoint), E-WRT-UICOPY-003–009, A11Y-007, G-001, G-003, PP-001, PP-003, PP-007. They remain in the audit pack for future triage but are not on this P0 list per operator decision.

## Audit Backlog — research 53 consent-free (2026-05-11)

Sourced from the consent-free deep-review at [`audit/53-consent-free-review.md`](audit/53-consent-free-review.md), an 8-reviewer + 1-aggregator fan-out against research 53 [`../jaan-to/docs/research/53-consent-free.md`](../jaan-to/docs/research/53-consent-free.md). 4 P0 + 14 P1 findings; full evidence + file:line citations in the report. The goal of this backlog is to unblock the **"customers deploy statnive.live without a consent banner under the CNIL Sheet 16 audience-measurement exemption, and operate compliantly in Germany under Section 25 TDDDG"** product story (research 53 §02 / §03 / §06 / §08). Three plan-pinned strengths from the audit hold and do **not** need rework: site-scoped daily-rotating BLAKE3 identity with multi-tenant test pinning, `/metrics` is PII-clean, and the sub-processor register is EU/DPF-only with zero telemetry exfiltration.

> **Cross-cutting theme — jurisdiction-aware per-site policy (resolves 3 P0s + 1 P1 with one workstream).** Add `SitePolicy.Jurisdiction` enum (`DE` / `FR` / `IT` / `ES` / `NL` / `BE` / `IE` / `UK` / `OTHER` / `IR`) at [`internal/sites/sites.go`](internal/sites/sites.go) + migration `010_sites_jurisdiction.sql`; auto-derive `ConsentRequired` / `RespectGPC` / `RespectDNT` / cookie-permitted defaults from jurisdiction at site-creation time (admin `POST /api/admin/sites` — currently exposes the flags via PATCH only, not POST). For `Jurisdiction=DE` force `ConsentRequired=true` + refuse `_statnive` cookie; for `FR/ES/IT/NL` permit the first-party analytics cookie under the CNIL Sheet 16 daily-salt server-side-hashing justification; for `IR` keep the current permissive defaults (Iran has no GDPR-equivalent statute per CLAUDE.md § Privacy). Closes **F-P0-2** (no jurisdiction defaults), **F-P0-3** (GPC/DNT default-off), **F-P0-4** (`_statnive` cookie unconditional in DE) in one slice, and unblocks **F-P1-14** (tracker JS reads a jurisdiction-aware policy hint from the `/api/event` 204 response). Pin with an integration test asserting `DE → ConsentRequired=true` at bootstrap. ~5 engineering days; the highest-leverage entry point for the consent-free claim. **Note:** F-P0-3 directly conflicts with CLAUDE.md Privacy Rule 6 (which deliberately set GPC/DNT defaults OFF after a 70–85% Brave/Firefox-strict/Safari traffic loss). Product + legal must decide whether to update Privacy Rule 6 (per research 53 §06 do-list) or accept the divergence and downgrade the consent-free marketing claim for any non-Iran site — the audit report's open question §Q3 frames the trade-off in detail.

### Activation model — universal vs. consent-mode setting

Three-bucket partition of the 18 findings, answering "what gets applied to every deployment vs. what the consent-mode toggle controls". This view is for product + marketing; the code-area groups below it are for engineering.

**Per-site policy (the toggle).** Two orthogonal admin-editable fields, set at site creation, editable later in `Admin → Site Settings → Compliance`:

- `Jurisdiction`: `DE` / `FR` / `IT` / `ES` / `NL` / `BE` / `IE` / `UK` / `OTHER-EU` / `IR` / `OTHER-NON-EU`
- `ConsentMode`: `consent-free` (CNIL Sheet 16 / Germany TDDDG strict) · `consent-required` (cookie banner) · `permissive` (current IR default per CLAUDE.md Privacy Rule 6)

`Jurisdiction` drives defaults and hard constraints (`DE` forces `ConsentMode=consent-free` with no cookie option; `IR` defaults to `permissive` but can opt in to any stricter mode). `ConsentMode` is the operator's chosen posture within those constraints. Site-creation wizard asks both questions; admin form lets operators flip later.

**Bucket 1 — Universal (always applied; no toggle).** Correctness, hygiene, or always-mounted routes. **9 findings:** F-P1-1 (referrer host-only), F-P1-2 (rollup TTL), F-P1-5 (salt big-endian), F-P1-6 (endianness consistency), F-P1-8 (LIA route), F-P1-9 (DPA route), F-P1-11 (audit constants), F-P1-12 (marketing wording on `statnive-website/`), F-P1-13 (event-audit admin endpoint). Each lands in any maintenance PR with no behavioural surprise for existing operators; none change tracker UX or dashboard counts.

**Bucket 2 — Mode-gated (consent-mode setting controls these).** The actual product-feature surface of the consent-free story. **6 findings:** F-P0-2 (the toggle's plumbing itself), F-P0-3 (`RespectGPC` / `RespectDNT`), F-P0-4 (`_statnive` cookie), F-P1-3 (round-to-10), F-P1-4 (3-event cap server-enforcement), F-P1-14 (tracker GPC client-probe).

| Lever | `consent-free` | `consent-required` | `permissive` (current IR) |
|---|---|---|---|
| `RespectGPC` / `RespectDNT` (F-P0-3) | **on** (forced) | on | **off** (CLAUDE.md Privacy Rule 6 unchanged for IR) |
| `_statnive` cookie (F-P0-4) | **DE: refused.** FR/ES/IT/NL: daily-salt server-side-hashing carve-out under CNIL Sheet 16 | post-consent only | permitted (current) |
| Aggregation to nearest 10 (F-P1-3) | **on** | off | off |
| CNIL 3-event-type cap (F-P1-4) | **server-enforced** (per-site `event_name` allowlist) | warned in admin | off |
| Tracker `navigator.globalPrivacyControl` probe (F-P1-14) | **active** | active | inactive (saves bytes) |

**Bucket 3 — Mode-aware infrastructure (route mounted universally; response varies with `ConsentMode`).** **3 findings:**

- **F-P0-1** `/privacy` page — route always mounted; rendered template varies (DE strict / CNIL exemption clause for FR-ES-IT-NL / generic consent-required / permissive IR disclosure).
- **F-P1-7** `/api/privacy/{opt-out,access,erase}` — endpoints always exist, CSRF + rate-limit + audit always fire; opt-out *semantics* differ (`consent-free`: writes strictly-necessary `_statnive_optout` flag cookie per research §08 carve-out + suppression-list entry; `consent-required`: clears existing consent cookie; `permissive`: still functional, expected traffic near-zero).
- **F-P1-10** Privacy-policy templates — both EN + DE shipped; site renders the variant matching `Jurisdiction × ConsentMode`.

**Why this partition matters for marketing.** Only Bucket 2 items become bullets under "Consent-free analytics — what's different" on `statnive-website/`. Buckets 1 and 3 are ambient product quality, not differentiation — `/privacy`, DSAR endpoints, host-only referrer, and rollup TTL are table-stakes for any analytics product, not features unique to the consent-free mode.

**Per-bucket implementation order recommended.** Bucket 1 first (low-risk maintenance PRs land any time, hardens the surface for every existing operator before the new modes go live) → Bucket 3 routes mounted (mode-aware but rendering only the current behaviour) → Bucket 2 cross-cutting workstream lands the `Jurisdiction` + `ConsentMode` enum and flips Bucket 3 routes into their mode-aware behaviour. Reverse order is also safe but produces a less defensible interim state.

### identity & enrichment (5 P1)

- [ ] **F-P1-1** [P1, enrich + storage] — `events_raw.referrer String DEFAULT '' CODEC(ZSTD(3))` at [`clickhouse/migrations/001_initial.sql:50`](clickhouse/migrations/001_initial.sql) persists full referrer URL for 180 days; [`internal/enrich/pipeline.go:169`](internal/enrich/pipeline.go) writes `raw.Referrer` unchanged even though `extractHostLower()` at [`internal/enrich/channel.go:484-509`](internal/enrich/channel.go) is already the canonical reducer. Call `extractHostLower()` before assignment; add Semgrep rule `referrer-host-only`. CNIL §02 verbatim: *"Le référent (« referrer »), s'il est collecté, se limite au domaine (« host »)."* → audit report § F-P1-1.
- [ ] **F-P1-2** [P1, retention] — Rollups `hourly_visitors`, `daily_pages`, `daily_sources` at [`internal/storage/migrations/002_rollups.sql:10-79`](internal/storage/migrations/002_rollups.sql) have no `TTL` clause — indefinite retention of `AggregateFunction(uniqCombined64, FixedString(16))` HLL state vs CNIL §02 25-month ceiling. Add `TTL day + INTERVAL 750 DAY DELETE` to all three. Pairs with the existing CLAUDE.md Recital-26 / C-413/23 anonymisation chain in [`docs/rules/privacy-detail.md`](docs/rules/privacy-detail.md). → audit report § F-P1-2.
- [ ] **F-P1-3** [P1, aggregation] — No aggregation-to-nearest-10 on visitor counts at [`internal/storage/queries.go:130, 162, 209`](internal/storage/queries.go) (`uniqCombined64Merge` returns exact). CNIL §02 verbatim: *"Agrégation et la présentation à la dizaine la plus proche. A défaut, une analyse est menée pour justifier du caractère anonyme des données."* Add `roundToNearest10(n int64) int64` helper at the API response layer for visitor cardinality fields only (not pageviews, not goals, not revenue). → audit report § F-P1-3.
- [ ] **F-P1-5** [P1, identity] — Salt derivation uses decimal-string `siteID` encoding at [`internal/identity/salt.go:142-147`](internal/identity/salt.go) (`fmt.Fprintf(mac, "%d||%s", siteID, date)`) instead of `binary.BigEndian` per [`blake3-hmac-identity-review`](.claude/skills/blake3-hmac-identity-review/README.md) skill spec. Tenants with sequential IDs (101, 102) get cryptographically separate salts only by accident of string representation. Replace with `binary.Write(mac, binary.BigEndian, siteID); mac.Write([]byte(date))`; pin byte layout with a regression test. No behavioural change because cross-tenant isolation already passes — this is regression-hardening. → audit report § F-P1-5.
- [ ] **F-P1-6** [P1, identity] — Endianness inconsistency: [`internal/identity/hash.go:69-71`](internal/identity/hash.go) (`UserIDHash`) uses `binary.LittleEndian.AppendUint32`; salt derivation uses string format. Pick one (recommend `BigEndian` network order across both paths) and anchor with a comment + a cross-file regression test. Lands in the same PR as F-P1-5. → audit report § F-P1-6.

### policy + cookies (3 P0 + 2 P1) — bulk closed by cross-cutting workstream above

- [ ] **F-P0-2** [P0, sites + admin] — No jurisdiction-aware per-site policy defaults. [`internal/sites/sites.go:62-69`](internal/sites/sites.go), migration `006_sites_privacy.sql:21-22`, bootstrap path at [`cmd/statnive-live/main.go:311-319`](cmd/statnive-live/main.go). Germany sites default permissive → Section 25 TDDDG hard-rule violation. *Closed by cross-cutting workstream above.*
- [ ] **F-P0-3** [P0, ingest + policy] — `RespectGPC` / `RespectDNT` default-off across all jurisdictions. [`internal/sites/sites.go:62-69`](internal/sites/sites.go), [`internal/ingest/handler.go:379, 383`](internal/ingest/handler.go), migration `006:21-22`. Conflicts with CLAUDE.md Privacy Rule 6 (deliberate choice — see cross-cutting workstream note). *Closed by cross-cutting workstream above if Privacy Rule 6 is updated; otherwise close by downgrading the marketing claim and routing F-P1-12 to website rewrite.* Severity reconciliation: P0 in DE / EU-default per research 53 §06; P1 elsewhere with documented mitigation.
- [ ] **F-P0-4** [P0, ingest] — `_statnive` UUIDv4 cookie set unconditionally on `ConsentRequired=false` sites at [`internal/ingest/handler.go:402-421`](internal/ingest/handler.go) (consent gate at lines 173-178 is correctly ordered, but the gate is bypassed by the default per-site policy). *Closed by cross-cutting workstream above (refuse cookie when `Jurisdiction=DE`; permit under CNIL first-party analytics-cookie justification for FR/ES/IT/NL with 13-month rotation).* Severity note: **P0 in DE; P1 in FR/ES/IT/NL with documented mitigation**. Audit report § Open Question Q1 (cookieID hashing) must be settled in the same PR — if raw `cookieID` is persisted in `events_raw.cookie_id` instead of hashed via the daily salt before write, F-P0-4 escalates globally and the research §05 anonymisation argument breaks.
- [ ] **F-P1-4** [P1, event cap] — Free-form `event_name` pass-through: [`tracker/src/tracker.js:103`](tracker/src/tracker.js) `.track(name, props, value)` accepts arbitrary `name`; [`internal/ingest/event.go`](internal/ingest/event.go) has no enum; goals use `MatchTypeEventNameEquals` so they don't expand the cap. CNIL §02's three-event cap is **semantic** (page presence / feature interaction / timing) — the server cannot enforce it today. Two paths: (a) constrain `event_name` to an enum mapped to the three CNIL categories (breaking change for power users), or (b) accept the operator's responsibility and route the marketing downgrade to F-P1-12. Pairs with F-P1-13 below. → audit report § F-P1-4.
- [ ] **F-P1-14** [P1, tracker] — Tracker JS at [`tracker/src/tracker.js:1-50`](tracker/src/tracker.js) does not introspect `navigator.globalPrivacyControl` defensively; GPC honoured server-side only and only when per-site policy opts in. Add a 5-line probe that short-circuits `sendBeacon` when `navigator.globalPrivacyControl === true` AND server policy honours GPC (read from the `/api/event` 204 response set by the cross-cutting workstream). Stays within the 1,478-byte minified / 747-byte gz tracker budget per [`preact-signals-bundle-budget`](.claude/skills/preact-signals-bundle-budget/README.md). → audit report § F-P1-14.

### privacy disclosure surface (1 P0 + 3 P1)

- [ ] **F-P0-1** [P0, landing + website] — No public `/privacy` route mounted in the binary. Route table scan at [`cmd/statnive-live/main.go:370-510`](cmd/statnive-live/main.go) returned zero `/privacy` matches; landing handler at [`internal/landing/**`](internal/landing/) serves only a coming-soon page. Render research 53 §08 EN + DE templates from `internal/landing/` (or a new `internal/legal/`) with locale negotiation; include the persistent Article 21 opt-out link required by §08. **Note:** Phase 11a-pre item 3 already commits to publishing `/privacy` on `statnive.com` / `.de` / `.fr` via the `statnive-website/` submodule; this finding extends the requirement to the binary's own surface (`app.statnive.live/privacy`) so the SaaS dashboard itself carries the disclosure, not only the marketing site. → audit report § F-P0-1.
- [ ] **F-P1-8** [P1, legal disclosure] — Public LIA template not published. EDPB Guidelines 1/2024 (adopted 8 Oct 2024) requires a "documented" three-step assessment. Serve research §07 LIA at `/legal/lia` (EN + DE); cite the Guidelines verbatim; review annually or on material change. Pairs with Phase 11a-pre item 9 (counsel review). → audit report § F-P1-8.
- [ ] **F-P1-9** [P1, legal disclosure] — No `/dpa` download route; [`docs/dpa-draft.md`](docs/dpa-draft.md) exists but is unmounted. Research §06 do-list verbatim: *"Provide a GDPR Article 28 DPA with EU-only sub-processors, downloadable without sales contact."* Mount `/legal/dpa` serving the existing draft as PDF + Markdown; gate by one-click acceptance form; emit `audit.EventDPADownloaded`. → audit report § F-P1-9.
- [ ] **F-P1-10** [P1, legal disclosure] — Privacy-policy templates from research §08 (EN + DE clauses) not served. Mount `/legal/privacy-policy/{en,de}` from the binary; pairs with F-P0-1 and Phase 11a-pre item 3 (the website mirror). → audit report § F-P1-10.

### privacy API + audit (2 P1)

- [ ] **F-P1-7** [P1, privacy API] — No `/api/privacy/{opt-out,access,erase}` endpoints. Route scan returned zero matches. Article 21 GDPR right cannot be exercised through the product. Add three handlers under a new `internal/privacy/`: opt-out writes a `_statnive_optout` strictly-necessary cookie (permitted under research §08 verbatim carve-out for opt-out flags) + server-side suppression entry keyed by `visitor_hash`; access returns the visitor's aggregated data rounded per F-P1-3; erase calls `internal/privacy/erase.go` enumerating `system.tables` dynamically per [`dsar-completeness-checker`](.claude/skills/dsar-completeness-checker/README.md). All three rate-limited by IP + CSRF-protected. Replaces the PLAN.md § Verification §15 placeholder ("`/api/privacy/erase` … lands with Phase 11a"). → audit report § F-P1-7.
- [ ] **F-P1-11** [P1, audit] — No `EventOptOut*` / `EventDSARErase*` / `EventDPADownloaded` constants at [`internal/audit/events.go`](internal/audit/events.go). Add now (no code dependency); wire into the F-P1-7 / F-P1-9 handlers when those land. Required for GDPR Article 28(3)(h) accountability. → audit report § F-P1-11.

### operator tooling + marketing (2 P1)

- [ ] **F-P1-12** [P1, marketing — website submodule] — Replace "qualifies under CNIL exemption" with "configurable to qualify under the CNIL exemption when deployed per the LIA + self-assessment" across `statnive-website/` and locale mirrors. Cite CNIL Sheet 16 verbatim. *Lives in [`statnive-website/`](../statnive-website/) and the auto-synced locale mirrors (`statnive-de` / `statnive-fr` / `statnive-jp` / `statnive-zh` / `statnive-fa` / `statnive-ar`), not in this binary.* Depends on F-P0-1..4 + F-P1-7/8/9/10 actually shipping in the binary before the claim text goes live. → audit report § F-P1-12.
- [ ] **F-P1-13** [P1, admin UI] — No operator-visible event-name audit endpoint. Add `GET /api/admin/event-audit` returning unique `event_name` cardinality per `site_id` over the rollup window; surface in the admin UI as "Event taxonomy" (current count vs. CNIL 3-category ceiling). Pairs with F-P1-4 — the audit endpoint is how an operator demonstrates compliance with whichever path is chosen (enum-cap or downgrade). → audit report § F-P1-13.

### Roadmap placement

| Phase / surface | Items | Notes |
|---|---|---|
| **Phase 11a-pre (GDPR ops gate — HARD GATE on first paying SaaS customer)** — extends the existing 9-item gate at § Phase 11a-pre | F-P0-1, F-P1-8, F-P1-9, F-P1-10 | Public-disclosure surface. Adds 4 routes to the binary (`/privacy`, `/legal/lia`, `/legal/dpa`, `/legal/privacy-policy/{en,de}`); aligns with item 3 (`/privacy` on the marketing mirrors) and item 9 (counsel review). |
| **Phase 11a (signup) — cross-cutting jurisdiction policy workstream** — HARD GATE before any "consent-free" marketing claim | F-P0-2, F-P0-3, F-P0-4, F-P1-7, F-P1-11, F-P1-14 | All three P0 policy findings + the `/api/privacy/*` endpoints + the audit constants + the tracker GPC probe. ~5 days for the policy enum + admin POST; +~3 days for the three privacy endpoints; +~1 day for tracker + audit constants. Settles audit Open Question Q1 (cookieID hashing) in the same PR. |
| **Maintenance — any near-term PR (low-risk, no behavioural change)** | F-P1-1, F-P1-2, F-P1-3, F-P1-5, F-P1-6 | Data-minimisation + identity encoding cleanup. None of these change tracker UX or dashboard counts (rounding is presentation-layer only); they harden the CNIL exemption surface. Land in the next identity- or storage-touching PR. |
| **v1.1 — admin polish + event taxonomy** | F-P1-4, F-P1-13 | F-P1-4 needs a product call (enum-cap breaking change vs. marketing downgrade); F-P1-13 is the admin endpoint that pairs naturally regardless of which path is chosen. |
| **`statnive-website/` (separate submodule + auto-synced locale mirrors)** | F-P1-12 | Marketing rewrite; depends on the in-binary items shipping first. Triggers `/statnive-de-sync` / `-fr-sync` / `-jp-sync` / `-zh-sync` / `-fa-sync` / `-ar-sync`. |

> **P2s deliberately NOT promoted to PLAN.md.** F-P2-1 (GeoIP `Get_all` → `Get_city` per [`geoip-pipeline-review`](.claude/skills/geoip-pipeline-review/README.md), Phase 8 deferred — functionally safe today, micro-allocation cleanup). F-P2-2 (Omnibus Article 88a(4) readiness — single-click refusal UX + 6-month suppression TTL behind a feature flag; 2027-2028 realistic adoption per research 53 §10). F-P2-3 (Cloudflare DPF certification date/expiry + supplementary-measures column in [`docs/compliance/subprocessor-register.md`](docs/compliance/subprocessor-register.md) — sub-processor register hygiene). F-P2-4 (Semgrep guard banning explicit `otel.Tracer(...)` / exporter instantiation to prevent a future contributor wiring an exporter via the transitive `go.opentelemetry.io/otel` indirect import — `air-gap-validator` extension). All four remain in the audit report for future triage.

## Twelve-Factor Compliance Map

statnive-live targets the [twelve-factor.net](https://twelve-factor.net) deployment posture (active 2024 fork at [github.com/twelve-factor/twelve-factor](https://github.com/twelve-factor/twelve-factor); 12 factors unchanged from 2011, strengthened guidance on secrets management / supply-chain / observability). The binary already complies; this map is the reference for future debate and for new contributors.

| # | Factor | Implementation in statnive-live | Status |
|---|---|---|---|
| I | Codebase | One git repo (`statnive-live/`), one binary, two postures (SaaS Netcup + air-gap Iran) | ✅ |
| II | Dependencies | `go mod vendor` checked in; `make vendor-check` blocks PRs that drift; `go-licenses check ./...` rejects AGPL | ✅ |
| III | Config | `viper` reads `config/statnive-live.yaml` + `STATNIVE_*` env vars; secrets via systemd `Environment=` drop-in at `/etc/systemd/system/statnive-live.service.d/env.conf` | ✅ (v1.1 SOPS+age option for git-tracked encrypted secrets — see § v1.1) |
| IV | Backing services | ClickHouse at `127.0.0.1:9000` via connection string (CLAUDE.md § Security #2); backups → Netcup Storage Box (SaaS) or local FS (air-gap), both swappable via YAML | ✅ |
| V | Build / release / run | **Build**: `make build` (Go static, vendored). **Release**: `make airgap-bundle` (binary + migrations + YAML + tracker + SHA256 + Ed25519 sig). **Run**: systemd `Type=simple` unit at [`deploy/systemd/statnive-live.service`](deploy/systemd/statnive-live.service) | ✅ binary; ⏳ automation in Phase 8b (`release.yml` + `deploy-saas.yml` + `courier-iran.sh`) |
| VI | Processes | Stateless Go binary; durable state in CH + WAL on disk; sessions in CH (Phase 2b) | ✅ |
| VII | Port binding | Binds `:443` directly via `CAP_NET_BIND_SERVICE`; no nginx reverse proxy | ✅ |
| VIII | Concurrency | Single binary, internal goroutines (6-worker enrichment, batch writer); horizontal scale = ClickHouse cluster at Phase 10 P5 | ✅ |
| IX | Disposability | Fast SIGTERM (graceful WAL drain ~1 s); SIGHUP hot-swap for TLS / GeoIP / channels / goals / alerts (Phase 7d) — most deploys never restart the process | ✅ |
| X | Dev/prod parity | `deploy/docker-compose.dev.yml` mirrors prod ClickHouse 24; same binary in CI integration tests + smoke + e2e jobs | ✅ |
| XI | Logs | Structured `slog` to stdout → journald; audit + alerts file-sinks at `/var/log/statnive-live/{audit,alerts}.jsonl` | ✅ (v1.1 vector.dev shipper to EU Loki — doc 29 §3.4) |
| XII | Admin processes | `statnive-live --migrate-only` runs migrations against same env; admin CRUD via `/api/admin/*` reuses live process | ✅ (v1.1 `statnive-live admin {migrate,rotate-salts,export-data,smoke}` subcommand surface) |

**Deploy automation seam.** Factor V is the only factor with non-trivial new work: Phase 8b adds the GHA harness above the existing bundle. Factors III + XI have optional v1.1 follow-ups (SOPS+age secrets, vector.dev shipper) that strengthen the posture without changing the binary. No factor requires retrofit.

**Iranian DC twelve-factor map**: identical to SaaS, with two clarifications. Factor I codebase ships the *same git tag* — the build (factor V) just runs outside Iran; the Iranian VPS receives the bit-identical artifact. Factor IV ClickHouse stays at `127.0.0.1:9000`, backups stay local FS (no Netcup Storage Box reachable). The courier protocol (§ Phase 10 P1 + [`deploy/courier-iran.sh`](deploy/courier-iran.sh)) preserves all 12 factors while crossing the air-gap exactly once per release.

## License Management (Self-Hosted)

Not open-source. Self-hosted customers need a license.

**v1 (Manual):** JWT `{site_id, customer, expires, max_events_per_day, features[]}` signed Ed25519. Startup: decode → verify signature → check expiry. File `config/license.key`. No payment system yet. Unlicensed = demo mode (30-day trial, 10K events/day cap, dashboard watermark).

**v2 (Automated):** license server at `license.statnive.live` (international SaaS only — D1 customers). Daily phone-home with 30-day offline grace. Payload strictly `{site_id, events_day_count, version}` — no PII. Polar.sh Merchant of Record. **D2 (Iranian DC) deployments NEVER reach this endpoint** — `license.phone_home=false` per Phase B config + `iptables -P OUTPUT DROP` end-to-end. Iranian customers stay on offline Ed25519 JWT (v1 model) indefinitely; v2 phone-home is a SaaS-only convenience.

**Key management (v1):** private key in age-encrypted file on offline laptop. Single keypair for all of v1; compromise = rotate + ship new binary. HSM + yearly rotation = v2 when volume justifies.

## v2 Roadmap (Post-Launch, +8–12 weeks)

| Feature | Effort | Priority | Lands |
|---------|--------|----------|-------|
| Sequential funnel (windowFunnel) | 2wk | High | v2 |
| Cohort / retention | 2wk | High | v2 |
| Filtering / drill-down | 2wk | High | v2 |
| Google Search Console | 2wk | High | v2 |
| Session tracking | 1wk | Med | v2 |
| Entry / exit pages | 1wk | Med | v2 |
| Engagement time / page gap | 1wk | Med | v2 |
| Telegram weekly reports | 1wk | Med | v2 |
| CSV export | 1wk | Med | v2 |
| Public REST API | 1wk | Low | v2 |
| **Operator CLI** | 1wk | Med | **v1.1** |
| **Statnive Query — read-only constrained SQL surface** | 2wk | High | **v1.2** |
| **MCP server** | 2wk | High | **v2** |
| Microsoft Clarity integration | 1d | Future | post-v2 |
| **LLM-triage prompt-injection defense (F10 bookmark)** | — | Conditional | **If-then** |

> **Statnive Query — v1.2 scope.** `POST /api/query/sql` admin-only HogQL-flavored SQL endpoint (doc 32 §A "expose SQL sooner" / doc 34 §D "copy in simplified form now"). Forced predicates: `WHERE site_id = ?` injected from session context; `AND time >= ? AND time < ?` required; row limit hard-capped at 500 (matching PostHog's cap). **Banned**: raw joins on `events_raw`, `SELECT *` on `events_raw.properties`, any mutation. `GET /api/query/schema` returns column list + types for `events_raw` + each rollup (schema browser). Ships with 10 seed `queries_templates` rows (unique users over time, funnel by two events, top referrers, top paths, retention matrix, etc.). Extends the existing `Store` + `Filter` + `whereTimeAndTenant` chain — read-only, so `tenancy-choke-point-enforcer` guards everything. Gives ~70% of PostHog HogQL power-user value at ~20% of implementation complexity.

> **Materialized hot properties — trigger, not scheduled** (doc 32 §A "query mechanics"). PostHog reports 55% avg / 25× p99 ClickHouse perf improvement after promoting heavily-read JSON properties to materialized columns. Statnive's 3 v1 rollups already do this at aggregate level; the future risk is `events_raw.properties` reads once custom-event volume grows. **Trigger**: when custom-event volume exceeds 20% of total ingest, **or** a single property read rate exceeds 1 k queries/hour, promote that property via `ALTER TABLE events_raw ADD COLUMN <name> <type> MATERIALIZED properties.<name>` migration. No action today; documented so the signal is recognized when it arrives.

> **F10 bookmark — if-then, not scheduled.** If a v1.1/v2 feature ever ships (a) an LLM-triaged error / crash-telemetry endpoint, (b) a GitHub-issue-bot that reads user-submitted issues, or (c) AI-assisted NL → ClickHouse query generation, adopt jaan-to-plugin research doc 77 §6–9: untrusted-input envelope (`<untrusted_input>` delimiter tags + explicit system-prompt override rules), CTQRS scoring (Completeness / Technical / Quality / Reproducibility / Severity), layered code-evidence validation, pre-integration sanitization (strip code blocks, credential patterns, shell metacharacters). Doc 30 anti-pattern already bans default-on `app_exception` telemetry, so (a) is unlikely in v1; (b) and (c) are post-v1. No skill, no test, no CI gate until a triggering feature enters scope. Revisit at v2 scoping.

## Skills & Tooling Surface

Authoritative inventory in [`docs/tooling.md`](docs/tooling.md) + the 14 `.claude/skills/*/README.md` specs. Research anchors: doc 23 (tooling landscape), doc 25 (install matrix + custom-skill catalog), doc 27 (three-gap closure: WAL / CGNAT / GDPR-on-HLL), doc 28 (GeoIP / Iranian-DC / CH-ops + upgrade playbook). Blacklist: `anthropics/skills/web-artifacts-builder` (air-gap violation), `shajith003/awesome-claude-skills`, `sickn33/antigravity-awesome-skills`, `rohitg00/awesome-claude-code-toolkit`.

## Brand & Design

Full spec — wordmark + summit logo, cream/ink/Persian-Teal palette, Fraunces + IBM Plex type ramp, token CSS, typography rules, voice rules, compliance hooks — in [`docs/brand.md`](docs/brand.md). Applies to statnive.live marketing + demo + tenant dashboards + SamplePlatform dashboard + README/docs.

**Marketing/brand PostHog-borrows (live in `statnive-website/` submodule, NOT this repo).** Cross-referenced here because M3 (public SaaS signup) depends on #1 + #3 existing before paid conversion matters. Source: docs 33 §3–§7.

- **Live pricing calculator** (doc 33 §5.3). Slider → exact bill, no "Contact Sales" button. Publish discounts publicly (25% OSS, 50% Iranian non-profits, 100% Iranian universities). Grandfather clause in writing: any price increase preserves existing pricing for 12 months minimum. — ~2 days in `statnive-website`.
- **"You'll hate statnive if…" homepage section** (doc 33 §4 #3). 5 bullets preempting objections: no session replay; no 30-products duct tape; no ads, no data resale; hosted in Iran; no sales calls. — ~1 hour.
- **Public handbook at statnive.live/handbook** (doc 33 §4.1). Strategy, pricing principles, refund policy, roadmap — all MDX in the Astro repo. Most raw material already exists in `jaan-to/docs/` + this file. — ~1 week initial pass.
- **Community Q&A + thumbs feedback on every docs page** (doc 33 §7). Giscus (self-hostable, GitHub Discussions backing) + 30-line JS thumbs → POST to `/api/event`. — ~1 day.
- **Dogfood the marketing site** (doc 33 §3.5). `demo.statnive.live` surfaces a read-only dashboard: pricing-page intent, install-doc completion, first-snippet-copy CTR, signup conversion funnel. Reuses the 8 existing `/api/stats/*` endpoints. — ~1 day.

## SaaS Model, Server Costs, Air-Gapped Deployment

**SaaS model (pricing tiers, multi-tenant architecture, GDPR requirements):** details in [`docs/deployment.md § SaaS Model`](docs/deployment.md#saas-model-statnive-live-cloud). Tiers: Free 10K PV ($0 self-hosted only), Starter 100K ($9/mo), Growth 1M ($19/mo), Business 10M ($69/mo), Scale 100M ($199/mo), Enterprise 1B+ (custom).

**Server costs:** Netcup VPS 2000 G12 iv NUE hourly-based (€25.48/mo + €5 setup, 8 vCore EPYC / 16 GB DDR5 / 512 GB NVMe / Nuremberg — **current D1 as of 2026-04-24** per research doc 36 with Hetzner-doc-verification fallback) Phase A dogfood → future Phase C growth-tier upgrade to Hetzner AX41 (~€39/mo) if doc-verification resolved OR Netcup Root Server RS 2000 G12 (~€22/mo prepaid / ~€30/mo monthly — Netcup-native path) → AX42 (€46/mo) or Netcup RS 4000 G12 for ~30–50 customers → AX102 (€104/mo) 100+. SamplePlatform Iranian DC ~€180/mo (quote-based; phase-dependent per [`deployment.md § Server Costs`](docs/deployment.md#server-costs)).

**Air-gapped / isolated deployment:** zero required outbound connections, single binary, all deps `go:embed` or vendored; opt-in external services (LE ACME, Telegram, license phone-home, GSC, Clarity, Polar, email) disabled by default. Full procedure (bundle contents, `make airgap-bundle`, install steps, prerequisites) in [`docs/deployment.md § Air-Gapped / Isolated Deployment`](docs/deployment.md#air-gapped--isolated-deployment).

## Launch Sequence

statnive-live ships in **three public-facing phases across two deployments**. Same binary, schema, config differences only.

| Deployment | Host | Tenancy | Purpose | Phases |
|---|---|---|---|---|
| **D1 — `statnive.live` (SaaS)** | Netcup VPS 2000 G12 NUE (v1, current — procured 2026-04-24) → Hetzner AX41/AX42 OR Netcup RS 2000/4000 G12 (Phase C growth) | Multi-tenant, pooled CH | Dogfood + public SaaS | A, C |
| **D2 — `SamplePlatform.statnive.ir` (Dedicated)** | Iranian DC (Asiatech / Shatel / Afranet) | Single-tenant (`site_id=1`), air-gapped | SamplePlatform production | B |

**Routing:** single tracker URL per deployment, hardcoded at install time — `statnive.live/tracker.js` for international SaaS, `SamplePlatform.statnive.ir/tracker.js` (and future `<tenant>.statnive.ir/tracker.js`) for Iranian customers. `site_id` resolved server-side from `Origin`/`Referer`. Path-based tenant routing in Phase C on D1: `app.statnive.live/s/<slug>/…`. **Two independent TLS cert chains** (one per domain — Architecture C consequence): (a) D1 `statnive.live` + `demo.statnive.live` + `app.statnive.live` via Let's Encrypt DNS-01 from outside-Iran (manual PEM, quarterly cron+SIGHUP); (b) D2 `statnive.ir` + `SamplePlatform.statnive.ir` via SamplePlatform internal CA OR Let's Encrypt DNS-01 issued on outside-Iran cert-forge then rsync'd inward (never ACME-from-Iran per `iran-no-letsencrypt-in-binary`). No wildcards in v1. Subdomain-per-tenant branding = v2 upsell.

**Auth per phase:** A demo = shared `demo/demo-statnive` viewer (displayed on login); B SamplePlatform = admin+viewer, rotatable via `/api/admin/users`; C SaaS = email+password, bcrypt + 14-day session.

**License per phase:** D1 (A + C) = no JWT (our instance; admin-user gating). D2 (B) = Ed25519 JWT at `config/license.key`, `MaxEventsDay=0`, `Features=["*"]`, `ExpiresAt=+1y`, offline.

### Phase A — Dogfood on statnive.com (Weeks 20–21)

D1 Netcup VPS 2000 G12 iv NUE hourly-based (€25.48/mo + €5 one-time setup, 8 vCore AMD EPYC x86_64 / 16 GB DDR5 ECC / 512 GB NVMe / 2.5 Gbit unlimited / Nuremberg, Germany / both IPv4 + IPv6 enabled / no contract lock-in — procured 2026-04-24 per doc 36 §4.1 fallback because Hetzner requires photo-ID doc-verification that can't be completed right now; tracker origin is public-facing, IPv6-only remains non-viable per doc 36 decision). DNS A/AAAA for `statnive.live`, `demo.statnive.live`, `app.statnive.live`. Manual PEM via `certbot certonly --manual --preferred-challenges dns` on laptop, drop on D1, quarterly cron+SIGHUP. LITE DB23. Config diff: `tls.{cert_file,key_file}` set; `license.required=false`. Seed: `INSERT INTO sites VALUES (1, 'statnive.com')`; shared viewer + internal admin. Tracker in `statnive-website/` Astro base layout. Login-attempt cap 10/min/IP. Banner: "Public demo — statnive.com traffic — viewer role, no writes".

**Acceptance:** 24h → non-zero visitors; viewer 403 on `/api/admin/*`; all 8 `/api/stats/*` return `site_id=1` data.

### Phase B — SamplePlatform dedicated Iranian VPS (Weeks 22–25)

Cutover scope = **SamplePlatform web only** (P1 onboarding, ~200K DAU / 3M views/day). D2 initial = Asiatech G1/G2 VPS (~15–28M Rial/mo). Graduates to dedicated bare-metal at P3 (~3mo post-cutover, +iOS). Hardware per sub-phase table in Phase 10. Install = offline bundle (`make airgap-bundle`) → SCP via bastion → verify SHA256+Ed25519 → `deploy/airgap-install.sh`. **DNS (Architecture C):** `statnive.ir` zone served by self-hosted NSD on AT-VPS-B1 (~7M Rial/mo separate from D2 origin); `A/AAAA SamplePlatform.statnive.ir → <Iranian-DC-IP>`. **No Cloudflare on `.ir`** (`iran-no-cloudflare`). Manual PEM (SamplePlatform internal CA preferred, or self-signed w/ distributed root, or LE DNS-01 issued from outside-Iran cert-forge then rsync'd inward — never ACME-from-Iran), quarterly. Upgrade to paid IP2Location DB23. License JWT + age-encrypted key. Config: `audit.sink=file`, `license.phone_home=false`. Seed SamplePlatform hostnames. Admin via secure channel. Tracker `<script src="https://SamplePlatform.statnive.ir/tracker.js" defer></script>` + root-domain cookie walking. Firewall: `iptables -P OUTPUT DROP` except loopback, CH localhost, tracker client IPs, DNS, NTP.

**Acceptance (P1 StreamCo MIN) — doc 29 §4.1 graduation gate:** 72h soak @ 240 EPS with diurnal curve, 7-scenario chaos matrix (BGP cut / mobile curfew / DPI RST / Tehran-IX degrade / Asiatech DC outage / clock skew / international-egress per doc 30 §3), 0→450 EPS breakpoint. Every SLO green (server loss ≤0.05%, client loss ≤0.5%, duplicates ≤0.1%, attribution ≥99.5% independently across 62% Iran + 38% diaspora cohorts per doc 30 §3, PII wire-scan rate()=0, p99<500ms, TTFB overhead ≤+25ms). Air-gap end-to-end + monthly backup+restore remain prerequisite. P2/P3/P4/P5 repeat the gate at 1K/4K/9K-18K/40K peak EPS before onboarding each successive app; P4 + P5 additionally require the long-session memory-leak soak (doc 30 §6, verification §48).

### Phase C — International SaaS self-serve (Weeks 25–29)

D1 continues Phase A on Netcup VPS 2000 G12 NUE; at ~10 paying, migrate to Hetzner AX41 (if doc-verification is resolved) or Netcup Root Server RS 2000/4000 G12 (Netcup-native path, no re-platform). New: `POST /api/signup`, `POST /api/billing/checkout`, `POST /api/admin/billing` (Polar webhook). Path-based tenant routing via chi middleware: `/s/<slug>/` → `site_id` context → scoped `/api/stats/*`. Missing slug → 404 / redirect to root login. Guardrails: DNS-resolvable, not blocklisted, unique, 5/hr/IP, email verification (24h grace). Free 10K PV/mo (v1 `count(DISTINCT)` → v1.1 `daily_users`); over-quota = soft throttle + `quota_exceeded=1` + upsell. Polar Products × monthly+yearly (Free self-hosted only, Starter $9, Growth $19, Business $69, Scale $199). **Merchant of Record** = no per-country tax registration. v1 Polar scope = checkout + webhook only; Portal + Benefits = v2. No Go SDK — call REST directly. Sandbox: `sandbox-api.polar.sh`. Onboarding at `/s/<slug>/onboarding` (user-triggered refresh). Email transactional opt-in via `email.enabled`.

**Acceptance:** signup → tracker → first event <5min; cross-tenant isolation (URL-manipulation blocked); Polar sandbox `subscription.created` → `sites.plan`, `subscription.canceled` reverts at period end; idempotent webhook; 6th signup/hr rejected.

## Key Files (Already Written)

All Go code from doc 22 is in the working tree. Per-package inventory in [`docs/repo-structure.md`](docs/repo-structure.md).

**License-verified deps (MIT/Apache/BSD/ISC):** clickhouse-go/v2 (Apache-2.0), go-chi/chi (MIT), go-chi/httprate (MIT), tidwall/wal (MIT), ip2location-go/v9 (MIT), medama-io/go-useragent (MIT), omrilotan/isbot (MIT), bits-and-blooms/bloom (BSD-2), lukechampine.com/blake3 (MIT), google/uuid (BSD-3), gopkg.in/yaml.v3 (MIT), filippo.io/age (BSD-3), klauspost/compress (BSD-3), bcrypt/acme/autocert (BSD-3), spf13/viper (MIT). ⚠️ hashicorp/golang-lru (MPL-2.0 weak copyleft — use unmodified). ❌ knadh/koanf (AGPL-3.0 — never use). ❌ pirsch-analytics/pirsch (AGPL-3.0 — never import). `go-licenses check ./...` must pass in CI.

## Technology Docs Cache

Context7-cached per-library API references (14 libs, 2026-04-17 snapshot). Full index + plan decisions that originated from the cache + API deltas (Vite Rolldown, JSX config, golang-lru v2, Vitest v4) in [`docs/tech-docs-index.md`](docs/tech-docs-index.md). Per-library files in [`docs/tech-docs/`](docs/tech-docs/).

## Verification

1. `go build ./cmd/statnive-live` compiles
2. `make test` passes (unit + integration)
3. `go-licenses check ./...` — zero AGPL / strong-copyleft
4. k6 load test sustains 7K EPS with p99 <500ms
5. All dashboard endpoints (8 stats + 2 admin + 1 realtime) scoped by `WHERE site_id = ?`
6. Multi-tenant isolation: site_id=A invisible in site_id=B queries
7. Enrichment order asserted: bot event → visitor_hash populated AND is_bot=1
8. Security: auth required, httprate 429, TLS 1.3 only, CH 127.0.0.1, hostname validation rejects foreign Origin
9. Crash recovery: kill -9 → restart → zero event loss (WAL replay)
10. CH outage: stop 10min → WAL buffer → resume → zero loss
11. Disk-full: 10GB cap → 503 with clear error, existing events preserved
12. Backup restore: row counts match exactly
13. TLS rotation: replace PEMs + SIGHUP → new cert without restart; expiry alert <30d
14. Tracker: install → events in dashboard <1h
15. GDPR (SaaS): consent decline drops cookies + user_id (shipped in v1); `/api/privacy/erase` visitor-wide eraser lands with Phase 11a per §36 — **not** yet exposed in v1 binary
16. License: demo-mode caps 10K/day; valid JWT unlocks; expired → demo-mode with warning
17. **Air-gapped acceptance**: offline bundle on `iptables -P OUTPUT DROP` host (loopback + tracker IPs). Binary starts, migrations apply, events ingest, rollups materialize, dashboard renders, backup+restore — zero outbound (skill: [`air-gap-validator`](.claude/skills/air-gap-validator/README.md))
18. **Offline build**: `go build -mod=vendor ./...` succeeds with `GOFLAGS=-mod=vendor`, no network
19. Manual TLS: binary serves with `tls.{cert_file,key_file}` internal-CA PEMs; no autocert code path (v1)
20. Air-gapped GeoIP update: replace BIN + SIGHUP → new IPs resolve without restart
21. **Pre-pipeline fast-reject** (doc 24 §Sec 1.6): handler 204 on `X-Purpose: prefetch`, UA <16 or >500, UA-as-IP, UA-as-UUID, non-ASCII — zero pipeline work
22. **Cross-day fingerprint grace** (doc 24 §Sec 1.1): visitor hashed 23:58 IRST salt S₁ returns 00:02 IRST → identified as returning via yesterday-salt lookup (skill: [`blake3-hmac-identity-review`](.claude/skills/blake3-hmac-identity-review/README.md))
23. **Bot ordering** (doc 24 §Sec 1.3): malformed UA / prefetch / spam referrer / outdated Chrome / regex-bot short-circuit at expected layer
24. **Central tenancy helper** (Rule 8): CI lint asserts every `SELECT` in `internal/storage/` calls `whereTimeAndTenant()` (skill: [`tenancy-choke-point-enforcer`](.claude/skills/tenancy-choke-point-enforcer/README.md))
25. **Schema time column**: `time` is `DateTime('UTC')` on `events_raw` + rollups (skill: [`clickhouse-rollup-correctness`](.claude/skills/clickhouse-rollup-correctness/README.md))
26. **Templated migration DDL** (doc 24 §Sec 2 Migration 0029): `{{if .Cluster}}` placeholders render for single-node + `ReplicatedMergeTree` + `Distributed` (skill: [`clickhouse-cluster-migration`](.claude/skills/clickhouse-cluster-migration/README.md))
27. **No Nullable columns** (Rule 5): CI lint — no `Nullable(` in `clickhouse/` or `internal/storage/migrate.go` (skill: [`clickhouse-rollup-correctness`](.claude/skills/clickhouse-rollup-correctness/README.md))
28. **Hostname lookup shape** (doc 24 §Sec 3.5): `map[string]struct{}` not `slices.Contains` — p99 <50 ns/call
29. **AI channel day 1** (doc 24 §Sec 3.3): `chat.openai.com` / `claude.ai` / `gemini.google.com` / `copilot.microsoft.com` / `perplexity.ai` → `channel="AI"`
30. **Day-of-week growth comparison** (v1.1, doc 24 §Sec 5 T2 #19): this-Tuesday-vs-last-Tuesday correct
31. **Phase A (dogfood)**: statnive.com pageview → `demo.statnive.live` <5min; shared viewer 403 on `/api/admin/*`; login cap 10/min/IP
32. **Phase B (SamplePlatform)**: `SamplePlatform.statnive.ir/tracker.js` → dashboard <5min; `iptables -P OUTPUT DROP` end-to-end on Iranian DC; backup+restore on dedicated instance; NSD authoritative on AT-VPS-B1 answers `dig SamplePlatform.statnive.ir @localhost` from inside Iran during simulated international cut
33. **Phase C (SaaS)**: signup → tracker → first event in `app.statnive.live/s/<slug>` <5min; cross-tenant isolation (URL-manipulation blocked); Polar sandbox webhook updates `sites.plan`; idempotent; 6th signup/hr rejected
34. **Kill-9 WAL gate** (doc 27 §Gap 1): CI 10K events → kill -9 random 100ms–2s → restart → `count() FROM events_raw == client 2xx` (within 0.05% SLO). 50 runs/PR. 7b close gate. (skill: [`wal-durability-review`](.claude/skills/wal-durability-review/README.md))
35. **CGNAT rate-limit tiering** (doc 27 §Gap 2): k6 `cgnat` = 7K EPS from 100 IPs simulating AS44244 — MUST NOT 429. `ddos` = 30K EPS from 50 IPs — MUST 429 (>50% fail). `normal` = 7K EPS from 10K IPs — <1% fail p99 <500ms. Phase 10 cutover gate. (skill: [`ratelimit-tuning-review`](.claude/skills/ratelimit-tuning-review/README.md))
36. **DSAR completeness** (doc 27 §Gap 3): synthetic `visitor_hash` → 100 events → `/api/privacy/erase` → WAL drain → `system.tables` enumerated dynamically → zero rows matching hash in every non-rollup table. New table without erase.go entry fails this test by construction. Phase 11 gate. (skill: [`dsar-completeness-checker`](.claude/skills/dsar-completeness-checker/README.md))
37. **Blackout-sim green** (doc 28 §Gap 2): vendored `-tags airgap` binary under `iptables -P OUTPUT DROP` (loopback + Docker bridge only). `/health/ready` within 30s, dashboard loads, 50 `POST /t` succeed, `/api/stats?range=1h` ≥50 pageviews, S3 backup degrades (`s3.*(unreachable|timeout).*continuing|degraded mode`), file-sink only (no `slack|pagerduty|opsgenie`). **HARD GATE** on every PR after Week 18. (skill: [`iranian-dc-deploy`](.claude/skills/iranian-dc-deploy/README.md))
38. **GeoIP hot-reload under load** (doc 28 §Gap 1): 100 concurrent lookup goroutines with `-race`; `SIGHUP` 100 times in 1s. p99 <500ms, zero FD leak across 1,000 swaps, last swap wins, zero lookup errors. 7K EPS k6 log grep for IPv4/IPv6 regex — zero matches. Gates Phase 10 paid-DB23 cutover. (skill: [`geoip-pipeline-review`](.claude/skills/geoip-pipeline-review/README.md))
39. **CH parts-ceiling + restore drill** (doc 28 §Gap 3): k6 5min 7K EPS — active parts <100, zero `RejectedInserts`, `DelayedInserts` <50, p99 `http_req_duration{kind:ingest}` <500ms. Nightly + labeled-PR `clickhouse-backup` create+upload+restore → row-count parity + `uniqCombined64Merge(uniq_state) FROM rollup_daily FINAL FORMAT Null` clean. Required before Week 23 load-rehearsal. (skill: [`clickhouse-operations-review`](.claude/skills/clickhouse-operations-review/README.md))
40. **Generator_seq oracle** (doc 29 §6.1): every synthesized event carries `(test_run_id, generator_node_id, test_generator_seq, send_ts)`; one ClickHouse query per run (loss / duplicates / ordering / latency) runs in <60s. Projection `proj_oracle` ORDER BY `(test_run_id, generator_node_id, test_generator_seq)` MATERIALIZEd on staging CH. (skill scheduled: `load-gate-harness`, Phase 7e)
41. **Per-phase graduation gate** (doc 29 §4): 72h soak + 7-scenario chaos + breakpoint 150% passes every SLO before tracker points at that phase. Binary sign-off ceremony with SamplePlatform analytics owner per sub-phase. No hand-wavy partial pass; any single SLO breach halts the gate. Target = doc 29 MAX envelope (200M events/day, 40K EPS burst at P5), not doc 30 observed — per Context "design ceiling vs observed" callout.
42. **7-scenario chaos matrix** (doc 29 §5 + doc 30 §3): A BGP cut (iptables NIN-only, 6h), B mobile curfew (tc netem 80% loss on mobile-AS srcs, 8h), C DPI RST (xt_tls on flagged SNI, 4h), D Tehran-IX degrade (iptables drop 185.1.77.0/24 + 60ms netem, 3h), E Asiatech DC partial outage (iptables drop DC subnet, 1h), F clock skew (block UDP 123 + date-drift, 4h), **G international-egress degraded (tc netem 80–120ms + 2% loss on outbound Tehran-IX / Asiatech → Frankfurt peering, 3h — pins the 38% diaspora cohort per doc 30 §3; oracle correlates `country != 'IR'` event loss independently of the 62% Iran cohort so attribution-correctness ≥99.5% holds for both cohorts, not just the weighted average)**. Each scenario ships with its tc/netem/iptables/xt_tls script AND its oracle SQL.
43. **Breakpoint ramp** (doc 29 §4): 0 → 150% of phase peak EPS over 30 min locates the failure knee above SLO ceiling. Not required to pass SLO above 100%; required to identify the knee for capacity planning and confirm graceful degradation.
44. **Production-replay protocol** (doc 29 §9 open item 4 + doc 30 §8): one anonymized NDJSON export per phase from SamplePlatform; cleanroom attestation signed by SamplePlatform analytics owner (regex-scrub spec + salt rotation per phase + auto-delete kill-switch post-sign-off); chain-of-custody from SamplePlatform bucket to statnive staging documented. **Seed distribution**: at least 40% of events target returning `visitor_hash` values per doc 30 §8 (SamplePlatform Week-1 retention 42.8%, Week-4 32.6%) — uniform-random under-exercises the bloom filter + cross-day fingerprint grace (doc 24 §Sec 1.1) path.
45. **PII wire-scan via Vector.dev + VRL** (doc 29 §3.4, §6.3): live <1s detection at 15K+ EPS (ipv4 / ipv6 / email / user_id regex redact); Loki stream `pii_leak=true`; Alertmanager fires on `rate() > 0` → gate halts. Supersedes one-shot [`test/pii_leak_test.go`](test/pii_leak_test.go) byte-scan.
46. **Staging cost envelope** (doc 29 §7): ≤40% of production monthly cost, billing-average — warm AT-VPS-B1 between gates; prod-parity during 72h hot window; ≤150% burst during breakpoint. All five phases satisfy the envelope at 17–37% per §7 table. Teardown scripted, not manual.
47. **Observability VPS separation** (doc 29 §3.2): Prometheus + Grafana + Pyroscope + Loki + Vector.dev on a dedicated AT-VPS on isolated rack/AZ from generators + target. Verified by `strace -f -e trace=connect` burn-in under `iptables -A OUTPUT -j DROP` except observability VLAN. Prevents softirq contention at peak from manifesting as phantom server-side regressions.
48. **Long-session memory-leak soak** (doc 30 §6): 1000 virtual users × 6h sessions × 1,080 progress-pings @ 20s cadence. Assert zero tracker JS memory leak across 6h, `visitor_hash` stable across cross-day salt rotation, zero duplicate `session_id` emission, all beacons correctly attributed. Exercises the long-Android-binge + mobile-web-power-user cohort (30% + 15% of sessions by count, majority of event volume) that doc 29's 4–15 events/session estimate missed — the heaviest mobile-web cohort averages 8h 26m engagement per doc 30 §6. Runs as part of P4 + P5 graduation gates.
49. **Diaspora-cohort load mix + SLO segmentation** (doc 30 §3): load generator emits 62% Iran-origin + 38% diaspora-origin sessions (19% Germany / 9% US / 7.5% Finland-VPN / 2.8% UK / 2.7% FR / 2.5% CA). Finland cohort tagged as Iranian-over-VPN with high-latency + high-jitter + VPN-egress-reliability profile (Lantern/Psiphon signature). Oracle SQL segments loss / duplicate / attribution SLOs per `geographic_cohort_id` in the generator_seq quadruple; all SLOs must hold independently on both cohorts, not just the weighted average. Pairs with chaos scenario G for the international-egress failure-mode coverage.
50. **Multi-tenant JOIN safety (F1, Phase 1+).** Every `JOIN` / subquery / CTE / `IN (SELECT …)` in `internal/storage/queries.go` carries `WHERE <joined>.site_id = ?` against the joined table, not just the outer predicate. Test fixture: synthetic two-tenant dataset (site_id=1 with 100 events, site_id=2 with 100 events), every `/api/stats/*` response queried as site_id=1 returns zero rows from site_id=2 even when a join against `sites` / `goals` / `funnels` / `daily_*` could cross-contaminate. Enforced by [`clickhouse-rollup-correctness`](.claude/skills/clickhouse-rollup-correctness/README.md) Semgrep rule `rollup-join-tenancy`. Complements [`tenancy-choke-point-enforcer`](.claude/skills/tenancy-choke-point-enforcer/README.md) (outer `WHERE`) — this covers the inner tables. (Pattern from jaan-to-plugin research doc 67; adapted for Go+CH from WordPress/PHP.)
51. **Outbound SSRF allow-list (F2, CLAUDE.md §Security #14 — planned v1.1, not yet shipped in v1).** `internal/httpclient/guarded.go` does not exist yet; v1 binary ships zero opt-in outbound paths so the guard is not load-bearing. Lands alongside the first opt-in outbound feature (ACME v1.1, Polar.sh webhook Phase 11b, or paid IP2Location download — whichever enters scope first). When shipped: all outbound `http.Client` / `net.Dialer` traffic routes through `internal/httpclient/guarded.go`. Unit test asserts: (a) FQDN not on `config.outbound.allowlist` → dial error; (b) RFC 1918 (`10/8`, `172.16/12`, `192.168/16`), loopback (`127/8`, `::1`), link-local (`169.254/16`, `fe80::/10`), and CGNAT (`100.64.0.0/10`) rejected post-DNS resolution (DNS-rebinding probe with a domain that resolves to `127.0.0.1` must be blocked); (c) `http://` scheme rejected, only `https://` accepted. Default `config.outbound.allowlist: []` preserves the air-gap Isolation invariant — the guard only applies when an operator opts in. Enforced by [`air-gap-validator`](.claude/skills/air-gap-validator/README.md) Semgrep rule `airgap-no-raw-httpclient` flagging any `&http.Client{}` / `http.DefaultClient` / `http.Get` / `http.Post` outside `internal/httpclient/`. (OWASP A10 mapping per jaan-to-plugin research doc 72 §11.)
52. **Mass-assignment guard on write endpoints (F4, Phase 3c + Phase 11).** Every mutating handler (`/api/signup`, `/api/admin/users`, `/api/admin/goals`, `/api/admin/billing`, future admin CRUD) decodes request JSON with `json.NewDecoder(r.Body).DisallowUnknownFields()` and enforces a per-endpoint `AllowedFields []string` whitelist pre-unmarshal. Sensitive fields (`site_id`, `role`, `is_admin`, `plan`) sourced exclusively from session context (or the verified Polar webhook payload for billing) — never from request body. Integration test: cross-tenant request body `{"site_id": 2, "role": "admin"}` submitted by a session-bound site_id=1 admin → handler writes site_id=1 (session value) and rejects the unknown/privileged fields; site_id=2 remains untouched. (Laravel mass-assignment pattern — jaan-to-plugin research doc 04 + CVE class in doc 67 — adapted to Go JSON unmarshaling.)
53. **Auth-return nil-guard (F5, Phase 2b).** Every caller of `internal/auth/**` functions with signature `(T *<Ptr>, err error)` guards `<ptr> != nil` after `err == nil`, not just `err != nil`. Regression test: a fault-injected `Authenticate()` implementation returning `(nil, nil)` is caught at every call site — not one handler allows request processing with a nil `*User` / `*Session` / `*License`. Enforced by [`blake3-hmac-identity-review`](.claude/skills/blake3-hmac-identity-review/README.md) Semgrep rule `auth-return-nil-guard`. (CVE-2024-10924 "Really Simple Security" pattern — jaan-to-plugin research doc 67 — applies to any Go `(*T, error)` return shape.)
54. **End-to-end boot smoke on every PR (Phase 5a-smoke).** `make smoke` via the `smoke-test` CI job drives the real `cmd/statnive-live/main.go` binary — not an `httptest.Server`, not a manually-wired `chi.Router` — against docker-compose ClickHouse (same container as `integration-tests` and `wal-killtest-smoke`) and probes every production surface: `/healthz` (JSON shape + WAL fsync p99 key), `/tracker.js` (IIFE + nosniff + `application/javascript`), `/app/` (CSP + nosniff + Referrer-Policy + `<div id="statnive-app">` + bearer placeholder rewritten), `/app/assets/*.js` (long-cache + body ≥ 5 KB), `POST /api/event` × 10 with `count() FROM statnive.events_raw WHERE site_id = <smoke>` count-back within 10 s, `/api/stats/overview` with bearer enforcement (401 without the header, 200 + 5 KPI keys with). Exercises `rateLimitMW` + `BackpressureMiddleware` + `dashboardAuthMW` from the prod router graph on every PR. Harness at `test/smoke/harness.sh`; pre-cutover canonical verification for Phase 10 per `docs/runbook.md` § Pre-cutover verification.
55. **Dashboard e2e on every PR (Phase 5c).** `npm --prefix web run e2e` via the `dashboard-e2e` CI job drives real Chromium against `bin/statnive-live` + docker-compose ClickHouse. Playwright `globalSetup` spawns the binary (same env-var contract as `test/smoke/harness.sh`), seeds two sites (801 + 802) + 40 synthetic events across device / channel / utm / country dimensions, then runs 25 tests across six spec files: `auth.spec.ts` (bearer injected, 401 without header, zero CSP violations), `panels.spec.ts` (every panel renders + Overview + Realtime assert CH-oracle numeric parity with HLL tolerance), `navigation.spec.ts` (hash dispatch, browser back/forward via pushState, invalid-hash fallback), `filters.spec.ts` (chip toggles + URL-hash preservation + custom date range), `realtime.spec.ts` (visibility-change pauses polling + restore triggers immediate fetch), `multitenant.spec.ts` (site switcher dropdown + site-switch CH-oracle parity on siteB). CH-oracle queries run via `docker exec clickhouse-client` wrapper at `web/e2e/fixtures/chOracle.ts`. Chromium-only (Firefox / WebKit add 3× runtime for near-zero Preact-specific coverage). Browsers cached via `actions/cache` keyed on `web/package-lock.json`. Expected wall time ~4 min. Trace retain-on-failure + `playwright-report` artifact on job failure. Pairs with Phase 5a-smoke (§54): smoke covers the shipped surface shape, e2e covers the interactive UI + numeric parity.
56. **GHA release pipeline green (Phase 8b — § Twelve-Factor Compliance Map factor V).** Pushing `vX.Y.Z` from a feature branch triggers `release.yml`. The published GitHub Release contains `statnive-live-vX.Y.Z-linux-amd64-airgap.tar.gz` + `.sha256` + `.sig`. `airgap-verify-bundle.sh tarball release-key.pub` exits 0. All actions SHA-pinned. Required GHA secrets (`STATNIVE_RELEASE_PRIVKEY`) stored encrypted in repo settings; never echoed in logs. Pre-existing `make release` gates (lint + test + test-integration + audit + airgap-bundle) all green before the bundle artifact is signed.
57. **GHA SaaS deploy green (Phase 8b — Phase 9 cutover).** Publishing the test release triggers `deploy-saas.yml`. The Netcup box's `/opt/statnive-live/current` symlink advances to `vX.Y.Z`. `curl -s https://app.statnive.live/api/about | jq -r .git_sha` matches the released tag's SHA. `curl -s https://app.statnive.live/healthz | jq -e '.clickhouse=="up" and .wal_fill_ratio<0.5'` exits 0 within 30 s of `systemctl restart`. Forced rollback (`gh workflow run rollback-saas.yml -f to=vX.Y.Z-1`) reverts the symlink + reload. Restricted `authorized_keys` `command="/usr/local/bin/statnive-deploy"` confirmed via `ssh -t deploy@netcup id` returning the deploy-script-only error rather than a shell.
58. **Iranian-VPS courier preserves air-gap (Phase 10 P1).** `make release-iran-vps` against a staging Asiatech-shaped VPS (outside-Iran + `iptables -P OUTPUT DROP` mimicking SamplePlatform) installs the bit-identical release tarball, leaves the box's outbound ruleset intact, `/healthz` returns `clickhouse=up` within 30 s. Running [`air-gap-validator`](.claude/skills/air-gap-validator/README.md) post-install over a 60-second observation window detects zero outbound DNS / TCP attempts from the binary. The outside-Iran bastion (the only network surface bridging GitHub + Iran) appears in [`docs/compliance/subprocessor-register.md`](docs/compliance/subprocessor-register.md) before first courier run. Post-install license JWT verifies (`statnive-live --license-only`), paid IP2Location DB23 BIN loaded (SIGHUP confirms `geoip.reloaded` in `audit.jsonl`), TLS PEMs cert-forge-issued (CLAUDE.md anti-pattern "no LE in Iran" upheld).
59. **Twelve-Factor parity gate (post Milestone 1 cutover postmortem PR-B).** `make config-parity-test` (or equivalent CI job) loads `config/statnive-live.yaml.example` through the binary's `loadConfig` and asserts that NO key in the file ends up at its hardcoded `v.SetDefault(...)` value — i.e. every key in the example is actually wired into the binary's viper schema. Catches the Milestone 1 cutover bug class where `audit.sink_path`, `clickhouse.dsn`, `geoip.bin_path`, etc. were silently no-ops because the binary expected different keys. Required gate before Phase 10 P1.
60. **Cutover acceptance gate — fresh-VPS install on >=2 distros.** Matrix CI runs `airgap-install.sh` + `step-b.sh` end-to-end against `ubuntu:24.04` AND `debian:13` Docker images. Both must reach `/healthz` `status=ok` `clickhouse=up` within 5 minutes of starting from a clean image, with no manual fixups. Catches the Milestone 1 cutover bug class where `iptables-restore` was missing on Debian 13, `./clickhouse install` didn't register the systemd unit on Debian 13, and netplan was assumed present. Required gate before Phase 10 P1.
61. **Consent-gate matrix (post Milestone 1, PR-E2 — Option C).** `internal/ingest/handler_test.go` asserts the 8-cell matrix: (a) SaaS defaults + no signal → no cookie + no `user_id_hash`, event still ingests; (b) SaaS defaults + `Sec-GPC: 1` → denied even with `X-Statnive-Consent: given`; (c) SaaS defaults + `X-Statnive-Consent: given` → cookie + hash both populate; (d) Iran posture (`consent.required=false`) + no signal → cookie + hash always set; (e) Iran posture + `Sec-GPC: 1` → still denied because `RespectGPC` is on; (f) `RespectGPC=false` + `Sec-GPC: 1` → ignored, cookie sets; (g) `DNT: 1` denies under SaaS defaults; (h) `RespectDNT=false` + `DNT: 1` → ignored. Closes LEARN.md Lesson 17. Required gate; runs as part of `make ci-local` Tier-3 via `make test`.
62. **GDPR ops gate (Phase 11a-pre — HARD GATE on first paying SaaS customer).** Items 1–9 in [§ Phase 11a-pre: GDPR ops gate](#phase-11a-pre-gdpr-ops-gate-hard-gate-on-first-paying-saas-customer): Netcup Art. 28(3) DPA signed, sub-processor register populated + linked from customer DPA, `/privacy` page published on .com/.de/.fr, MX null + SPF + DMARC + DNSSEC, LUKS on `/var/lib/clickhouse`, NTP pinned to Netcup, HSTS preload + monthly testssl, `vm.swappiness=1`, **counsel review sign-off** in `docs/compliance/counsel-review.md` covering DPA + privacy page + sub-processor register + cookie posture. None of these block the **Milestone 1 functional** gate (which closes when the 24h soak passes — the binary works); all of them block the **first paying SaaS signup**. Independent track; can run in parallel with Phase 11a code work.
63. **Probe parity (Phase 8b follow-up, LEARN.md Lesson 20).** Spin `bin/statnive-live` with `STATNIVE_SERVER_LISTEN=0.0.0.0:18443` + a self-signed cert; run `deploy/statnive-deploy.sh probe`; assert exit 0 + emits `healthz: ok` within 30 s. Catch case: bare `STATNIVE_SERVER_LISTEN=0.0.0.0:18080` (no TLS), probe must derive `http://` scheme. The `derive_healthz_url` shell function in `deploy/statnive-deploy.sh` parses `server.listen` + checks for `tls.cert_file` in `/etc/statnive-live/config.yaml`; the four-scenario locally-verified matrix (TLS prod / HTTP dev / env override / missing config) ships in `fix/v0.0.1-rc1-deploy-followups`. Required gate before next release tag.
64. **Crawler embed size assertion (Phase 8b follow-up, LEARN.md Lesson 23).** `bin/statnive-live --check-embed-sizes` prints the byte size of every `//go:embed` blob in the binary and exits 1 if any falls below its release-build floor (`crawler-user-agents.json` floor = 100 KB / `crawlerJSONMinBytes`). Wired into `make airgap-bundle-verify` so a stale-embed regression fails at bundle-verify time, not silently in production. Pairs with the runtime ERROR-level startup log in `internal/enrich/bot.go:NewBotDetector`. The unit test `TestCrawlerEmbedSize` asserts the same floor on every `make test`. Catches the v0.0.1-rc1 GHA-build regression where `//go:embed` resolved to an empty copy of the JSON despite the file being 254 KB on disk.
65. **Auto-deploy chain green (Phase 8b follow-up, LEARN.md Lesson 22).** After `fix/v0.0.1-rc1-deploy-followups` merges, push the next test tag. Assert `release.yml` succeeds AND `deploy-saas.yml` fires automatically via the new `gh workflow run` step in `release.yml`'s tail (no manual `gh workflow run` needed). Replaces the manual `workflow_dispatch` step in the cutover SOP. `release.yml` requires `actions: write` permission for the dispatch.
