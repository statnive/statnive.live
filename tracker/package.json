{
  "name": "statnive-tracker",
  "version": "0.1.0",
  "description": "statnive.live first-party JS tracker — vanilla IIFE, ≤1.5 KB minified",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "test": "vitest run --environment=jsdom"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "jsdom": "^25.0.1",
    "rollup": "^4.30.1",
    "vitest": "^4.1.5"
  }
}
